- 在 `Assets/Editor/MissionDataEditorWindow.cs` 数据编辑器中新增「异常状态」(AboState) Tab，加载 `Assets/Resources/GameData/AboState/` 下的 `AboStateData_SO` 资产。Tab 展示 `typeEnum`(DamageTypeEnum，用 GetInspectorName 取中文名)、`icon`、`color` 三字段，按 `typeEnum` 排序，遵循现有 Tab 模式（列表项+右侧 Inspector 编辑）。
- 为 `Health` 类新增 partial 文件 `Assets/Scripts/02Game/Game/Shared/Health.AboState.cs` 处理异常状态逻辑。新增内部类 `AboGaugeEntry`(Current/Max/LastGainTime/Source/VfxInstance) 替代原 `Dictionary<DamageTypeEnum, SKVP<int,int>> AboGauge`。`AboStateData_SO` 已由用户补充 `duration`/`recovery`/`damage`/`vfx` 字段。机制：HandleDamage 增加积蓄并刷新 LastGainTime；满槽后维持 duration 才开始按 recovery*_AboTick(0.5s) 衰减，未满直接衰减；满槽每 tick 由 Source 造成 Real 伤害（TakeDamage），并通过 VFXManager.Creat 把 vfx 挂到造成伤害者(Source)下。新增公开方法 `IsAboStateFull(DamageTypeEnum)`。`Health.cs` 的 `HandleDamage` 签名改为接收 damageSource，`TakeDamage` 调用处传入；空 `AboTick()` 移至 partial 实现。
