- 补完 `Assets/Scripts/04UI/UI/HpItemBase.cs` 的异常状态 UI 显示。在 `Health_AboState.cs` 中新增公开结构 `AboStateViewInfo`(Type/Current/Max/Icon/Color) 与方法 `GetActiveAboStates(List<AboStateViewInfo>)`，复用传入列表避免 GC，仅返回积蓄值>0 的状态（icon/color 取自 ResSvc.aboStateDic）。`HpItemBase` 新增 `_aboStates` 缓存列表与 `RefreshStates()`，在 `Set`/`Refresh` 中调用：非零状态数 > States 现有子物体数则实例化 StatePrefab 补充，小于则隐藏多余物体；预制体第0子物体为 fill Image（fillAmount=Current/Max），第1子物体为图标 Image（替换为状态图标）；比例<1 时 fill=状态色/图标白色，比例==1 时 fill 白色/图标=状态色。
- 新增 `Assets/Scripts/02Game/Gameplay/SPEffect/GroundFireEffect.cs`，继承 `SustainedEffect`（命名空间 `FPSGame.Gameplay`，属 Assembly-CSharp，无独立 asmdef）。激活时以 `Collider.bounds.center` 为圆心、`DamageData.GetDamageOuterRadius(1).RawFloat`（即 ExplosionRange）为半径检测附近同类型实例。逻辑为「先查后登记」：若范围内已存在其他 GroundFireEffect，则停止自身 `ParticleSystem.Stop(true, StopEmitting)` 且不登记入缓存（视为隐藏）；否则登记入缓存。为避免遍历全部实例，使用静态网格空间分区：`Dictionary<long, List<GroundFireEffect>> s_Grid`，键为 `PackCell(x,y)=((long)x<<32)|(uint)y`（支持负坐标），CellSize=10，扫描格数 `FloorToInt(range/CellSize)+1` 保证覆盖检测圆。倒序遍历单元格列表以安全清理无效引用。同时把基类 `SustainedEffect` 的 `Collider`/`DamageData` 改为 `protected`、`OnEnable` 改为 `protected virtual` 供子类复用。
