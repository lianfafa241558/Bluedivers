# 2026-07-11

## 武器升级编辑器窗口
- 创建 `Assets/Scripts/02Game/Editor/WeaponUpgradeEditorWindow.cs`
- 左侧列表：图标+名称（右上）+类型（右下）展示 Resources/Weapons 下 WeaponPlayerController
- 右侧上部：升级树采用"竖向行=等级，横向项=选项"布局，每行显示 Level 标签 + 横向排列的选项卡片，支持拖拽添加、点击选择、删除等级/选项
- 右侧中部：模组卡片，显示 ModuleType 类型颜色、激活标记★、右键菜单
- 右侧底部：选中 SO 的详情面板
- 菜单入口：Bluedivers > 武器升级编辑器

## 武器热量系统
- `WeaponAttrType` 新增 HeatPerShot(13)、CoolDelay(14)、CoolSpeed(15)
- `WeaponAttributeFactory` 注册三项配置（HeatPerShot/CoolDelay 带 FlipPlus）
- `WeaponCfg.TypePara` 基础列表加入三项属性，所有武器默认拥有
- `WeaponBaseController` 新增热量系统：
  - `_currentHeat` (0-100)、`IsOverheated` (公开只读)、`_coolTimer` (散热延迟计时器)
  - `HandleShoot()` 过热线检查 + 射击后累加热量（>=100 触发过热）
  - `UpdateHeatSystem()` 每帧执行：CoolDelay 结束后按 CoolSpeed 散热，热量归零解除过热

## ModifyAttrData 检视器 modifier 下拉框过滤（最终方案）
- `ModifyAttrData` 去掉 `[Singleline]` → EditorOverride 不再手动展开子字段，走默认 PropertyField
- `ModifyAttrDataPropertyDrawer`（`[CustomPropertyDrawer(typeof(ModifyAttrData))]`）:
  - `OnGUI()` 四字段内联 + modifier 根据 `attributeConfigs[type].Item3` 过滤
  - 无需任何 EditorOverride 特殊处理，纯标准 Unity PropertyDrawer 机制

## 伤害类型系数 WeaponAttrType
- `WeaponAttributeFactory.cs`: 新增 12 个伤害类型枚举值 DamageTypeGun(200) ~ DamageTypeHacker(211)，对应 DamageTypeEnum 的每种伤害类型
- `WeaponAttributeFactory.cs`: 注册工厂配置 `(false, AttrTag.Percentage | AttrTag.OneHide, ModifierType.All)`
- `DamageData.cs`: `GetAttr()` 新增 12 个 case 映射到 `DamageGroupDirect.GetValue(DamageTypeEnum.Xxx)`
- `DamageData.cs`: 新增 12 个 setter `SetDamageTypeXxx(PEInt)` → `DamageGroupDirect.SetValue(DamageTypeEnum.Xxx, value.RawFloat)`
- `WeaponPlayerController.cs`: `InitAttribute()` 新增 12 个 `Set(Attr.DamageTypeXxx, damages.SetDamageTypeXxx)` 绑定
- `WeaponPlayerController.cs`: `UnInitAttribute()` 新增 12 个 `OnFinalValueChange -= damages.SetDamageTypeXxx` 解绑

## 爆炸伤害组伤害类型系数
- 新增 ExplosionTypeGun(212)~ExplosionTypeHacker(223) 12 个枚举值，对应爆炸伤害组 DamageGroupExplosion
- 工厂配置、GetAttr/setter（读写 DamageGroupExplosion）、InitAttribute/UnInitAttribute 绑定全部对称添加
- 直击(DamageTypeXxx → DamageGroupDirect) + 爆炸(ExplosionTypeXxx → DamageGroupExplosion) 共 24 个伤害类型系数属性

## WeaponUpgradeEditorWindow 空引用/越界修复
- `DrawUpgradeLevelRow`: `FindPropertyRelative("Key")`/`("Value")` 可能返回 null（KVP 结构体内 List 未初始化时），添加 `if (keyProp == null || valueProp == null) return;`
- `AddUpgradeLevel`: 新元素 `FindPropertyRelative` 添加 null 检查
- `RemoveUpgradeOption`: `upgradeProp` / `valueProp` 添加 null 检查
- `HandleDragDropOnRow`: `upgradeProp` / `valueProp` 添加 null 检查
- `GetSelectedSO`: `upgradeProp` / `valueProp` 添加 null 检查
- `CreateNewUpgradeOption` / `AddUpgradeSOToLevel`: 同上模式
