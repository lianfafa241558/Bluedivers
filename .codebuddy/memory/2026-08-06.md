# 2026-08-06 工作日志

## 全工程调研 + skill/记忆落地
用户要求：通读整个工程库，总结已实现/未实现（对标《绝地潜兵2》+《深岩银河》），指出可改进架构，并说明 KCP 迁移影响。最终将总结写入 skill 和记忆。

### 调研结论
- **项目定位**：合作 PvE 第三人称/第一人称射击割草，由 Unity FPS Sample 二次开发，命名空间 `Unity.FPS.*`。
- **已实现**：完整的伤害/属性/武器/投射物/AI/波次/地形/任务/战备/昼夜系统，以及事件驱动 + 双层定时器 + 固定逻辑帧 `LogicBehaviour` + `PEMaths` 定点数框架。
- **未实现**：联机同步（最关键）、重型战备、洞穴体素、地形挖掘、割草大密度、货运链、多人进度。
- **KCP 迁移影响面小**：业务代码 Photon 依赖仅 `SingletonNet`/`NetManager`/`BridgeSys`（2 个 RPC）+ `BridgeWnd`/`ArmamentWnd`（仅 using）。战斗层走固定逻辑帧 + 定点数，天然契合确定性同步。
- **架构改进优先级**：① 确定性帧同步 + 统一随机源 ② DTO 化解耦 Unity 类型 ③ 拆分 WaveManager ④ 统一命名空间/asmdef ⑤ 清理 UnityEditor 误引 ⑥ 重写 RPC 封装 ⑦ God Class ⑧ 查询网格池化 ⑨ 洞穴（可选）。

### 写入文件
- `.codebuddy/skills/bluedivers-unity/SKILL.md`：更新项目定位、KCP 网络约定（替换 Photon 章节）、补充关键基础设施表（LogicTimerSystem/WaveManager/UnitQueryGrid/PEMaths/GameAttribute/WeaponController）、避坑补充（RPC 封装 bug、随机源未收口）。
- `.codebuddy/skills/bluedivers-unity/references/module-guide.md`：追加「项目全景总结」章节（已实现/未实现/KCP 迁移要点/架构改进/下一步）。
- `.codebuddy/memory/MEMORY.md`：更新项目环境（Photon 弃用→KCP）、新增项目定位、新增「项目全景总结」精简版。
