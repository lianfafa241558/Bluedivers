# 2026-08-10

- 修改 `Assets/Scripts/04UI/UI/HpItemBoss.cs`：BOSS 血条按玩家距离控制显示。新增 ShowDistance=50f、FadeSpeed=4f 常量。Set() 中不再无条件隐藏，而是按初始距离决定：一开始在 50m 内则 alpha=1 直接显示并立即播 showClip（置 _hasShown=true），否则 alpha=0 隐藏。Tick() 中用 Mathf.MoveTowards 在 GetAlpha/SetAlpha 间做淡入淡出：玩家（ActorsManager.Player.Pos）在 50m 内淡入(target=1)，超过则淡出(target=0)。showClip 仅在第一次完全淡入时播放一次（_hasShown 防重）。IsPlayerNear() 判空 player 与 actor。
