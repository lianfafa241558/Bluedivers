# 2026-08-18

- 完成 `Assets/Scripts/Effect/EffectComp/LookPlayer.cs`：新增 `LookPlayer` MonoBehaviour，功能为让物体总是注视主相机。在 `LateUpdate` 中 `transform.LookAt(相机)`，相机引用通过 `_cameraTransform` 缓存，判空时 `RefreshCamera()` 重新获取，避免每帧 `Camera.main` 查找开销。
