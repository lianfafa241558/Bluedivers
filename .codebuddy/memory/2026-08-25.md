# 2026-08-25

## ArchiverDataHandle.cs：ArchSettingDataDrawer 去折叠平铺
- `Assets/Editor/Drawer/ArchiverDataHandle.cs` 中 `ArchSettingDataDrawer`（`ArchSettingData` 的 PropertyDrawer）去掉了 `EditorGUI.Foldout` 折叠箭头，改为直接平铺显示所有字段。
- 标题行用 `EditorGUI.LabelField` 显示，子字段（titile/type/value + 按 type 展开的 showTexts 或 sliderRange/sliderSuffix）始终绘制。
- `GetPropertyHeight` 移除 `property.isExpanded` 判断，始终返回完整高度。
- 常量 `FoldoutIndent` 更名为 `IndentWidth`。
- 进一步改造：`value` 字段按 `SettingBtnType` 动态渲染控件——`Dropdown` 显示 Popup（选项取 `showTexts`，value 存索引）；`Slider` 显示滑动条（范围 `sliderRange`，写回 `F2` 字符串）；`Toggle` 显示 bool 复选框（写回 `"0"`/`"1"`）。读取用 `float.TryParse` 兜底 null。`GetPropertyHeight` 分支与新绘制行数一致。
