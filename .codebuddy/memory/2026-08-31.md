# 2026-08-31

## DeployableMine 高空单位误引爆修复
- 根因：治疗无人机 `HealdroneBase.prefab` 等飞行单位 `halfHeight: 0`，原 `InVerticalRange` 对 `HalfHeight <= 0` 直接放行（"0=不做高度过滤"约定），导致高空掠过仍引爆。
- 修复：`InVerticalRange` 重写为 `InTriggerRange` 实际 3D 距离判定——竖直间隙按 [CenterPos.y - HalfHeight, CenterPos.y + HalfHeight]（未配置退化为中心点本身），水平间隙 = 中心距 - actor.HalfRange（保巨型单位黏附可引爆），两者合成 sqrt 后与触发距离比较。
- 隐患提示：halfHeight=0 的**地面**单位（如 Dec_Bane/Dec_Roach/Dec_Front/Dec_Back）现在按中心点算竖直距离，若其 AimPoint 离地超过触发距离(默认0.7)会炸不到，需用批量工具补 halfHeight。
- `VerticalRange` 字段语义改为"实际3D距离容差(0=同触发范围)"，Inspector 名改为"触发距离(0=同触发范围)"。

## Damageable.cs 方法注释补全
- 为 `Assets/Scripts/02Game/Game/Shared/Damageable.cs` 所有缺失 summary 的方法补充 XML 注释：`GetArmorRatio`、`SetIsMain`、`Awake`、`ClosestCollider`、`InflictDamage`、`ArmorDestruction`、`DestroyPart`、`Tick`、编辑器 `AutoSetting`（已有注释的方法如 `ExplosionBlocking`/`RestoreArmor`/`BreakArmor`/`IsFriendlyUnit` 未改动）。

## PlayerWeaponsManager 武器槽位映射重构（枚举与槽位解耦）
- 背景：`WeaponTypeEnum` 新增 Support/Empty，原"槽位 = (int)枚举值"的隐式约定失效。
- 新映射（`PlayerWeaponsManager` 顶部静态表）：主0/副1/**支援2/特殊3**/投掷4/信号枪5/空手6/护甲7(占位)。提供 `SlotOf(WeaponTypeEnum)`、`SlotToWeaponType[]`、`IsSwitchableSlot(int)`、`MaxSwitchableSlotIndex=3`、`SwitchableSlotCount=4`。
- 滚轮/数字键只在槽位 0-3 切换（数字键 1-4）；投掷/信号枪保持各自呼出方式（G键/蹲键）；`SwitchWeapon` 循环与 `GetDistanceBetweenWeaponSlots` 回绕均限定 SwitchableSlotCount。
- `AddWeapon` 改为按 `weaponPrefab.WeaponTypeEnum` 映射槽位落位（依赖 `WeaponPlayerController.WeaponTypeEnum` 字段在 Inspector 正确配置），槽位被占返回 false。
- 家具操作（`OnOperation`）由原来的 FlareGun+1（旧 Armor 占位槽）改为切到 `SlotOf(Empty)`；恢复时用 `IsSwitchableSlot(m_LastWeaponIndex)` 判断，不可切换则回槽 0。
- 连带修改：`PlayerWnd.cs:289` 手雷槽位改用 `PlayerWeaponsManager.SlotOf(...)`。
- **遗留提醒**：`PlayerWnd.cs:122-126` 武器栏 UI 仍只显示槽位 0-2 图标（新映射下是 主/副/支援，特殊武器在槽3），如需显示4把需 UI 加子项。
- 支援武器空槽排查（2026-08-31）：滚轮/数字键（`GetWeaponAtSlotIndex()!=null` 守卫）、`SwitchWeapon`（跳过空槽）、`ReplaceWeapons`/`ChangeWeapon`（null 武器 → 状态 Down）均安全；唯一 NRE 点是 `PlayerWnd` 初始化设图标循环，已加 `item ? item.WeaponIcon : null` 判空（`SetSprite` 传 null 仅清空 Image，安全）。注意 `PlayerWnd.AddWeapon` 回调是空实现，捡起支援武器后图标不会自动刷新，实现捡起功能时需补。

## 支援武器拾取 + 轮盘卸载系统（2026-08-31，已完成）
- **背景**：支援武器初始不在玩家槽中，进战斗后以地面实体拾取；可经"丢弃装备"轮盘（EquipmentUninitiatedUI + InputState.Equip，**不新增输入键**）卸载回落地面再拾取。
- **方案**：不新写 WeaponPlayerController 子类，用 partial 分部文件实现 IEquippable（规避 Owner 冲突）。
- **新增文件**：
  - `02Game/Game/Shared/Weapon/WeaponPlayerController_Equip.cs`：partial 实现 IEquippable。`I_Actor IEquippable.Owner` 显式实现（规避基类 Owner(GameObject)）；`OnInstall`→`wm.EquipGroundWeapon(this)` 入槽；`OnUninstall`→`wm.DetachWeapon(this)` 出槽 + `SetParent(null)` 落地到玩家面前 1m + 恢复缓存层级(m_CachedLayer) + `ResetForPickup` + re-enable pickup 组件；`OnEquipDestroy` 事件在 OnDestroy 触发。
  - `02Game/05Interactable/Furniture_WeaponPickup.cs`：继承 Furniture_Attached。覆写 ShowName/Id/Icon（武器无 I_Actor，基类取 _actor 会 NRE）；`CanOperate`=基类条件 && Support 类型 && 玩家支援槽为空（`unit.GetComponent<PlayerWeaponsManager>()` + `GetWeaponAtSlotIndex(SlotOf(Support))==null`）；`Operate` 用 inOperate 区分：false=拾取(InstallEquip)+置 inOperate=true；true=轮盘卸载(UninstallEquip)。不调 base.Operate() 避免 FurnitureOperate 副作用。`ResetForPickup` 复位 canOperate/inOperate/owner/pressTime。
- **修改文件**：
  - `PlayerWeaponsManager.cs`：AddWeapon 重构=Instantiate + EquipGroundWeapon(实例,槽位)；新增 `EquipGroundWeapon(weaponInstance)` 入槽(复用 AddWeapon 的父子化/图层/升级/自动切换/OnAddedWeapon) + 入槽后 `pickup.enabled=false` 脱离交互列表；新增 `DetachWeapon`(出槽+OnRemovedWeapon+SetWeaponState(false)断订阅+若主手则 SwitchWeapon(true)，不 Tool.Destroy)。
  - `PlayerWnd.cs`：AddWeapon/RemoveWeapon 空实现补全——按 weaponIndex 刷新 `weaponList.transform.GetChild(weaponIndex,0)` 图标（卸载清空），注意用 `weaponList.transform.childCount`。
  - `PlayerOperationController.cs`：第一人称射线 `hit.transform.GetComponent<IFurniture>()` → `GetComponentInParent`（兼容 Collider 在武器子物体）。
- **重要前提/坑**：轮盘卸载调 `Furniture_WeaponPickup.Operate()` 时不经过 Handle，`owner` 依赖拾取时残留的玩家引用（拾取走 Handle→owner=user）。若 owner 为空卸载会失败。
- **工作区路径修正**：实际工作区为 `d:\Pro\Bluedivers`（用户环境快照里写的 e:\Bluedivers 过时）。
- **用户需手动配置**（AI 未动资源）：支援武器预制体 ①根物体挂 `Furniture_WeaponPickup` ②加 Collider（可在子物体，射线已改 GetComponentInParent）③`WeaponTypeEnum=Support` ④武器名/图标 ⑤`PickupName`（可选，为空用武器名）⑥场景摆放地面支援武器实体验证。验证：交互键拾取→槽2图标出现→按"丢弃装备"轮盘→点卸载[武器名]→武器落地可再拾取。
