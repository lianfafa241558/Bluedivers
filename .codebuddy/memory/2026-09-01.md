# 2026-09-01

- LimbEditorWindow（肢体编辑器）预览空白排查：现象为中间预览一度全灰底（已自愈），代码自 8/23 提交后未变。结论：瞬态失效，最可能是 BeginPreview/EndPreview 之间某帧异常导致 `m_previewOpened` 卡死（域重载/重开窗口自愈）或编辑器图形设备重置。已给用户两个加固建议（未实施）：① RenderPreview 的 catch 里调 `DestroyPreview()` 重建；② `RenderModelMeshes` SMR 分支 `BakeMesh` 输出已是世界坐标，再乘 localToWorldMatrix 属双重变换（identity 节点无影响，带旋转/缩放节点会飞出视野），可直接删二次变换。

- 新建 `Assets/Editor/EnemyStatEditorWindow.cs`（菜单 Tools/敌人/怪物编辑器）：读取 Assets/Resources/Prefabs/Enemy 下带 Actor 的预制体，左列表显示 Portrait+ShowName，右侧基础血量输入框（默认 Health.MaxHealth）+ 血量表（行=ExtraDifficulty[3] 固定0~3，列=8难度，公式参照 HealthEnemy.Awake：×(1+Extra[3]*0.1)×难度系数）+ 每武器每 DamageData 伤害表（行=ExtraDifficulty[0] 固定0~3，公式参照 FpsHelper.DiffDamageScale 但**不含玩家人数项**：×(1+Extra[0]*0.15)×难度系数，BulletFlag.EnemyIntensify 决定是否增幅）。DamageData 私有字段 DamageDirect/DamageExplosion 用反射读取；纯预览不写回预制体；文件已转 UTF-8 BOM。（2026-09-01 二次修改：用户要求伤害不随玩家数变化、ExtraDifficulty 最大固定 3、删掉两个输入框；并修复了 PowerShell 转码导致的中文乱码——PowerShell 5.1 `Get-Content -Raw` 按 GBK 读无 BOM UTF-8 会毁文件，必须 `[System.IO.File]::ReadAllText($p,[Text.Encoding]::UTF8)` 显式指定）（2026-09-01 三次修改：改为可写回预制体——名称 ShowName/ID/头像 Portrait（Actor，BaseObject 基类字段）+ 基础血量（Health.MaxHealth 且同步 showHealth），修改即时 Undo.RecordObject+SetDirty，工具栏加"保存资产"按钮落盘 AssetDatabase.SaveAssets）（2026-09-01 四次修改：支持 上/下箭头键 切换列表选中项——OnGUI 开头 `EditorGUIUtility.editingTextField` 为 false 时拦截 KeyDown，自动滚动用 Begin/EndScrollView 计算可见范围；注意 `EditorGUILayout.BeginScrollView` 返回的是 Vector2 滚动位置而非 Rect，滚动区域矩形要在 EndScrollView 后用 `GUILayoutUtility.GetLastRect()` 取；右侧详情路径标签可点击，`EditorGUIUtility.PingObject` 在 Project 窗口定位预制体 + `AddCursorRect(MouseCursor.Link)` 手型光标；注意 GetLastRect 点击检测必须保留 LabelField 占布局位，删掉标签改手动 GUI.Label 会导致 GetLastRect 取到上一个控件矩形而重叠）
- 关键知识点：敌人武器 = `GetComponentsInChildren<WeaponBaseController>(true)`（同 EnemyController.FindAndInitializeAllWeapons）；伤害难度增幅开关 = `WeaponBaseController.BulletFlag.HasFlag(BulletFlag.EnemyIntensify)`（投射物据此设 ProjectileHitData.useDiffScale）；HealthEnemy 血量缩放用 `ExtraDifficulty[3]`，伤害用 `ExtraDifficulty[0]`（Task/ExtraDifficulty 为 int[4]）。

- 手持装备 IK 增加手肘 Bend Goal：`PlayerMountPoint.SetHandIK` 签名扩展为 `(lHand, rHand, lElbow = null, rElbow = null)`，通过 `fullIK.solver.leftArm/rightArm.bendGoal` + `bendGoalWeight` 设置（有肘点权重 1，无则 0），ClearHandIK 同步清空；`HandEquip` 新增 `LElbow`/`RElbow` 字段（InspectorName 中文标注）并传入 SetHandIK。

- HandEquip 层级修复（第一人称可见性）：第一人称看手部走 `PlayerWeaponsManager.WeaponCamera`（cullingMask 只含第一人称武器层 FirstPersonWeapon），HandEquip 在 Default 层导致第一人称不可见。修复：OnInstall 挂载后递归切到 `LayerDefinition.WeaponLayers`（用户确认是单一武器层，掩码转索引同 PlayerWeaponsManager.EquipWeapon 的 `Mathf.Log` 写法），OnUninstall 落地后按 `m_CachedLayer` 递归恢复（先例：WeaponPlayerController_Equip）。持有状态固定该层即可，两视角都正常，无需订阅视角切换。项目层序号：FirstPersonWeapon=10、MainCameraIgnore=11。
- 视角切换角度 bug（已修）：第一→第三人称时相机瞬移回上次退出第三人称的旧角度。根因：第一人称水平角直接转角色 transform（BaseSelfController.HandleRotation），`_cameraYaw` 不更新；第三人称全靠 `_cameraYaw` 驱动；切回第一人称用的是角色 transform.yaw 所以正常，切到第三人称的跳转块却用旧 `_cameraYaw`。修复：`PlayerController.LateUpdate` 切换跳转块开头加 `_cameraYaw = transform.eulerAngles.y;`（同死亡相机 L494 的处理）。注意 `_cameraYaw` 与死亡相机语义：死亡时取 `transform.eulerAngles.y - 180`（相机在身后）。
- 潜在冲突（未修，已告知用户）：`GameRoot.Awake` L112 `LayerDefinition.FirstPersonIgnoreLayers = firstPersonIgnoreLayers` 会覆盖 `LayerConfigInitializer.Awake` L47 的 `weaponLayers|firstPersonIgnoreLayers`，两个静态初始化器抢同一属性，依赖脚本执行顺序。

- `PlayerWeaponsManager.UpdateWeaponAiming()` 修改：第三人称瞄准时武器固定使用 `DefaultWeaponPosition.localPosition`，不移动到 `AimingWeaponPosition`。改动是在瞄准分支内根据 `m_PlayerCharacterController.IsThirdPerson` 选择目标位置——第三人称取 `DefaultWeaponPosition.localPosition`，第一人称保持原 `AimingWeaponPosition.localPosition + activeWeapon.AimOffset`。FOV 处理不变（第三人称 zoomRatio=1 不缩放）。

- 修复第三人称切换时模型"转半圈"bug：根因在 `PlayerWeaponsManager.LateUpdate` L423 用 `Mathf.Lerp(ModleRoot.localEulerAngles.y, m_PlayerAngle, t)` 做角度插值，`Mathf.Lerp` 是纯数值线性插值、不感知 0/360 环绕，当模型朝向（0-360）与武器侧身角 `m_PlayerAngle`（-90~90）跨 0 点且数值差 >180° 时走长弧转半圈以上。修复：改用 `Mathf.LerpAngle`（自动取最短弧）。注：`PlayerController.LateUpdate` L329 `_cameraYaw = transform.eulerAngles.y` 是瞬跳赋值，不是转半圈来源。`_cameraYaw` 是累积量不归一化，但切第三人称时会被 L329 重置，故无问题。

- **PlayerWeaponsManager 的 IK 统一迁移到 PlayerMountPoint（2026-09-01）**：移除 `PlayerWeaponsManager` 的 `m_fullIk`（`FullBodyBipedIK`）字段，改持 `m_MountPoint`（`PlayerMountPoint`）。双持判断用 `m_MountPoint.IsLeftHandFree/IsRightHandFree`；主武器切枪用 `m_MountPoint.SetHandIK(LHand, RHand)`；副武器（左手）用 `m_MountPoint.SetLeftHandIK(LHand)`。`PlayerMountPoint` 新增 API：`EnsureIK()`、`SetLeftHandIK(Transform)`、`IsLeftHandFree`/`IsRightHandFree`。**时序注意**：`SetStatrtWeapon` 在 `PlayerController.SetBody` 内部调用（L276），早于 `SetBody` 末尾的 `OnBodySet` 事件，因此 `SetStatrtWeapon` 获取 `m_MountPoint` 后须调 `m_MountPoint.EnsureIK()` 兜底解析 `fullIK`（否则初始武器 IK 被跳过）。迁移前 `m_fullIk = GetComponentInChildren` 在 SetStatrtWeapon 里主动取能拿到（模型已 SetParent），迁移后改组件引用需 EnsureIK 保证时序。
- **手持 HandEquip 期间禁止滚轮/数字键切武器（2026-09-01）**：`EquipController` 新增 `IsHoldingHandEquip()`（遍历 `equips` 字典检查是否含 `HandEquip` 类型，`key is HandEquip`，需 `using FPSGame.Gameplay`）。`PlayerWeaponsManager` 缓存 `m_EquipController`（Awake `GetComponent<EquipController>()`），`UpdateTrySwitchWeapon` 切换入口处 `if (m_EquipController.IsHoldingHandEquip()) return;` 禁止切换。注意：之前实现"切武器→自动丢下 HandEquip"（`HandEquip.OnWeaponSwitched` 订阅 `OnSwitchedToWeapon`）现在因禁止切换而不会触发，但保留作为兜底（如数字键/其他切换路径）。

- **HandEquip 落地模拟重力（2026-09-01）**：为手持装备卸载落地加基于 CharacterController 的重力，复用现成组件 `Assets/Scripts/Effect/EffectComp/CCGravity.cs`（此前无调用方，命名空间 EffectComp，`05_EffectComp.asmdef` autoReferenced=true）。改动仅在 `HandEquip.cs`：① 类加 `[RequireComponent(typeof(CharacterController), typeof(CCGravity))]`；② `Awake` 缓存 `m_Gravity`/`m_Controller`，并把物体上非 CC 的 Collider 设为 `isTrigger=true`（第一人称拾取靠 `Physics.Raycast` 命中、默认命中 Trigger，`queriesHitTriggers` 项目未改仍为 true；CC 作为落地唯一实体物理体避免双碰撞）；③ `OnInstall` 禁用 `m_Gravity.enabled`/`m_Controller.enabled`（挂手部不受重力/不碰撞）；④ `OnUninstall` 落地（`SetParent(null)` 后）`m_Gravity.ResetVerticalVelocity()` + `enabled=true` + `m_Controller.enabled=true`。两条落地路径（被动丢下 Drop / 轮盘卸载 Furniture_HandEquip.Operate else 分支）都走 `OnUninstall`，启停闭环一致。因 RequireComponent，Unity 会自动补挂组件，无需手动。
- **修正：HandEquip 落地重力改为完全内联（2026-09-01 二次）**：用户选择"完全内联到 HandEquip"以消除 02→05 跨层反向依赖，取代之前复用 CCGravity 的方案。改动：`HandEquip.cs` 移除 `using EffectComp;`、`[RequireComponent]` 只留 `CharacterController`、删除 `CCGravity m_Gravity` 字段；内联新增 `GravityDownForce=20f`/`MaxFallSpeed=50f`（public + `[InspectorName]`/`[Tooltip]`）、私有 `m_VerticalVelocity`/`m_GravityActive`、`Update()` 内联下落（等效 CCGravity：`isGrounded && vel<0` 时 `vel=-1` 贴地停，否则 `vel -= Gravity*dt` 限速后 `m_Controller.Move`）。`Awake` 仍把非 CC 的 Collider 设 `isTrigger=true`（第一人称 raycast 拾取）。`OnInstall`：`m_GravityActive=false`+`m_VerticalVelocity=0`+`m_Controller.enabled=false`；`OnUninstall` 落地后：`m_VerticalVelocity=0`+`m_GravityActive=true`+`m_Controller.enabled=true`。初始地面态 `m_GravityActive` 默认 false（静止摆放不下落），仅卸载落地后下落。`CCGravity.cs` 保留但无调用方。编译 0 错误，02→05 反向依赖已消除。

- **雷达站完成暴露全图任务（2026-09-01，终版=遍历方案）**：放弃全局事件方案（用户明确指出不必加事件），改为 MissionLidarStation.OnKeyScreenComple 在 CompleteMission() 后直接遍历 `BattleManager.Instance.MissionCont.missions`（MissionController 的 missions 列表已改 public，含所有 MissionBase 实例），对每个 `!mission.end` 且 `mission.entity.IsValid()` 的任务调用 `mission.entity.TryDiscovered()`（entity 是 MissionBase 第 106 行 public MissionView 字段）。BattleEventSub/MissionView 的事件改动已全部回退干净。经验：项目 MissionController.missions 本身就维护了全量任务列表，此类"广播给所有任务"的需求优先遍历该列表，不用加全局事件。
