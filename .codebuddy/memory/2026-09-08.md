# 2026-09-08

- 系统安装 Git 2.55.0（winget 被临时文件锁阻塞后改用用户手动下载的 D:\Git-2.55.0-64-bit.exe 静默安装 /VERYSILENT /NORESTART，装到 C:\Program Files\Git）。系统原有 TortoiseGit（TGitCache 进程）。
- 已配置全局 GitHub 清华镜像加速：url."https://mirrors.tuna.tsinghua.edu.cn/git/github.com/".insteadOf "https://github.com/"（后因 TUNA 镜像非全量代理、找不到个人仓库已 unset；直连 GitHub Connection reset，用户网络无法直连）。
- URPFog 包（moe.meryuhi.effects.fog）已本地嵌入 Packages/Fog/（用户手动复制，绕过 GitHub 拉取）。为其新增 HeightAndDistance 组合雾模式：shadergraph 由用户手动连线（Mode 关键字加 HEIGHTANDDISTANCE 枚举项 + 第二个 Mode KeywordNode 开关 + _HeightParams(Vector2) 属性 + saturate((startH-worldY)*inv) 高度因子子图，乘到 Density Saturate 之后的 Redirect 输出上）；C# 侧 FullScreenFogMode 枚举 + UseHeightParams + RendererFeature 写 _HeightParams。初版 bug：Subtract.B 忘接世界 Y（World Position 组 Split.G）导致高度因子恒为常数，表现与纯距离雾一致，已修复。
- EnvironmentLightingModule.cs（DayNightSystem）新增全屏雾 Volume 驱动：fogVolume 字段 + 6 条按 NormalizedTime 采样的曲线（强度/密度/距离起止/高度起止），雾色复用现有 fogColor 渐变，Initialize 时 profile.TryGet<FullScreenFog> + 全参数 overrideState=true + 强制 HeightAndDistance 模式；用 volume.profile（运行时实例）避免污染资产。

