# 2026-09-09

- 天气雾合成模型修正：乘法（曲线×倍率）在昼夜曲线低点时失效（0.2×0.1=0.02 没效果），改为 FogIntensityAdd 增量直接叠加强度、VisibilityMultiplier 只收缩距离。WeatherEffect 新增 _calmFogAdd/_stormFogAdd 字段。关键：WeatherEffectRain 无周期风暴，暴雨参数要配在"平时"那组字段。后续补充联动：高度雾起/终点随能见度整体抬升（+(1-vis)×fogHeightRise，新字段默认8米，Y坐标不能乘倍率否则雾反向下沉）、内置雾 RenderSettings.fogDensity 也 ÷vis 加浓。
- 天气切换瞬间跳变修复：WeatherAtmosphereController 拆分 Target*(天气写入)与当前值(消费方读取)，Smooth(deltaTime) 每帧 Lerp 渐变（TransitionDuration 由 EnvironmentLightingModule Inspector"天气氛围过渡时长(秒)"默认3秒写入），Smooth 由昼夜 Tick 驱动。天气亮度联动最终方案：太阳+月亮+环境光三者都联动。CelestialVisualsModule 太阳/月亮强度叠乘 SunIntensityMultiplier（WeatherEffect _calm/_stormSunIntensity 默认风暴0.4）；EnvironmentLightingModule 环境光三色叠乘 AmbientBrightnessMultiplier（_calm/_stormAmbientBrightness 默认风暴0.45）。注意太阳强度所有权在 CelestialVisualsModule（昼夜淡出），环境光在 EnvironmentLightingModule（仅 Trilight 分支）。地面发黄的另一染源：fogColor/skyColor/groundColor 渐变夜间关键帧若为暖黄色需改冷蓝灰（待用户自查渐变）。
- 天空盒 _Lerp 驱动：Environment/DayNightPanoramic 的 Day-Night Lerp 原本无人驱动恒为1（永远白天）；EnvironmentLightingModule 新增 skyboxDayNightLerp 曲线（默认白天1/夜晚0/日出日落0.5各6%过渡）每帧 SetFloat("_Lerp")，并乘天气 SkyLerpMultiplier（WeatherEffect _calm/_stormSkyLerp 默认风暴0.3，暴雨白天天空压向夜晚全景图）。连带修复体积云昼夜色（DrawVolumetricCloud 读天空盒 _Lerp）。
- 雾色压暗：雾比环境亮违和感修复——全屏雾 color 与 RenderSettings.fogColor 都乘 AmbientBrightnessMultiplier（雾照明来自环境，语义=fogColor渐变定义本色×天气遮挡倍率=最终色）。云 shader 已重写为程序化 FBM 并加云团聚（clusters 调制阈值），用户参数建议：云朵尺度0.1/基础云量0.5/边缘柔和度0.18/密度1.3。
- VolumetricCloud.shader（WalkingFat/VolumetricCloud_URP_Fixed）整体重写为程序化 FBM 体积云：世界坐标 XZ 驱动（堆叠 quad 共享云场）、云量=基础云量(材质 _CoverageBase)×_CloudCoverage(天气)、伪光照（偏移采样密度差）、昼夜云色 lerp(_NightCloudColor,_DayCloudColor,_CloudLerp)×Tint×Exposure、垂直 hFade。保留旧属性名兼容 DrawVolumetricCloud 脚本（_CloudDayTex 等声明但不再使用）。注意：材质 Colud1 旧序列化 DstBlend=One（加亮混合）需手动改 OneMinusSrcAlpha，否则云发白发亮。
- 天气渐变加固：WeatherAtmosphereController.Smooth 加帧保护（Time.frameCount，一帧多消费方调用只驱动一次）；DrawVolumetricCloud.Update 也调用 Smooth 兜底（昼夜系统没跑时渐变照常）。月亮强度也联动 SunIntensityMultiplier（CelestialVisualsModule 太阳+月亮都乘）。
- DrawVolumetricCloud 性能+光照优化：矩阵数组缓存（层数变化才重建）消除每帧 GC、Camera.main 缓存、视锥锥形裁剪跳过不可见层（_cullMarginDegrees 默认20°余量，visibleCount 传入 DrawMeshInstanced）；新增太阳/月亮 Light 引用，-forward 传 shader _SunDirection，伪光照偏移方向跟随真实天体（太阳落下切月亮）。

- CelestialVisualsModule.cs 淡出改为按角度驱动：fadeDuration(秒) → fadeAngleSpan(度，InspectorName"淡出角度跨度"默认15)，delta = |DeltaAngle(上帧角,当前角)| / span，Initialize 记录 _lastAngle 防首帧跳变。
- FullScreenFogEditor.cs：组合模式补显示高度字段（UseHeightParams(mode)），全部字段加中文 GUIContent 标签（模式/强度/雾颜色/衰减模式/距离雾起止/高度雾起止/衰减系数/噪声组）。注意 End Line 仅 Linear 密度模式显示（指数模式无终点概念，原包设计）。
- 天气-氛围联动架构：新建静态桥 WeatherAtmosphereController.cs（**位于 Assets/Scripts/DayNightSystem/，属 DayNightSystem.asmdef 程序集**——初版放 01Manager/Battle 导致 EnvironmentLightingModule 无法反向引用 Assembly-CSharp，已迁移；故意保持全局命名空间，Assembly-CSharp 侧 WeatherEffect 免 using）。字段：VisibilityMultiplier/CloudCoverageMultiplier/CloudBrightnessMultiplier。WeatherEffect 基类新增"氛围联动"字段组（平时/风暴×能见度/云覆盖/云亮度），OnInit/OnStormStart/OnStormEnd 时 ApplyAtmosphere 写入。EnvironmentLightingModule.UpdateFullscreenFog 合成：强度×倍率、距离×倍率、密度÷倍率(参考 0.05 下限+Clamp01)、高度不变。DrawVolumetricCloud：OnEnable 缓存云贴图基础 Tiling，Update 应用覆盖倍率(SetTextureScale)和亮度倍率(×_CloudExposure)，新增 _useWeatherLink 开关。语义约定：Start Line=距离雾起点(米)、End Line=终点(仅Linear生效)、Start/End Height=世界Y、Color.a 运行时被 Intensity 覆盖。
