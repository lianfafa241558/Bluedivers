# 2026-09-15

## 修复：敌人受击闪白"执行到一半就停住（残留亮斑）"
文件：`Assets/Scripts/02Game/AI/FxCont/RendererSet.cs`

### 现象
MPB 触发器渐变（`EFD_Enemy.asset` 条目2：type=Trigger/occasion=Hit/colorName 空→`_HitColor`/duration=0.1，渐变 黑→灰0.5(0.23)→灰0.5(0.8)→黑(1.0)）有时播不完、有时停在"亮的部分"不动。

### 根因
`Update()` 只在 `(Time.time - lastTriggerTime) <= Config.duration` 时写颜色，**超时后没有收尾分支**。
`Renderer.SetPropertyBlock` 是把块**拷贝存到 Renderer** 上，之后没人写就一直留着 → 最后一帧算出的颜色（progress ∈ (1帧/duration, 1)，即 0.83~1.0 的渐变尾段，亮度 0~0.42 灰）被**永久钉住**。触发相位随帧随机 → 表现为"有时候"。
另：`duration` 很小时首帧就超时 → 一帧都不写，整个闪变被跳过（同源问题）。
隐含冲突：同一 Renderer+materialIndex 只有一个 PropertyBlock，多条目（EFD_Enemy 条目2/3 都 `material: 无`）会整块互相覆盖，残留在别的条目写入时才被顺手抹掉。

### 修复
- 新增 `private bool playing` 显式播放态；`Trigger` 命中时置 true。
- `Update` 首行 `if (!mpb.IsValid() || !playing) return;`
- 两个分支都用 `Mathf.Clamp01` 求进度，**超时那一帧也写一次终点色**（Trigger=`gradient.Evaluate(1)`，Switch=`Color.Lerp` 收敛到目标色），随后 `playing=false`。
- 抽出 `ApplyColor(Color)`（SetColor + ApplyToRenderers + mpb.Clear）；Switch 的魔数 2 提为 `const float SwitchDuration = 2f`。

### 相关常识
- `_HitColor` 在 `ToonLit_Shared.hlsl:409` 是**自发光叠加**（`result = _HitColor;`），默认 `(0,0,0)`＝无闪光，故终点写黑即"无效果"。
- `mpb.Clear()` 在 `ApplyToRenderers()` 之后是安全的（SetPropertyBlock 是拷贝语义）。

## 重构：MPB 所有权从"条目"下沉到"渲染槽位"（解决多条目互相整块覆盖）
文件：新增 `Assets/Scripts/02Game/AI/FxCont/RendererSlot.cs`；改 `RendererSet.cs`、`EnemyControllerFX.cs`

### 问题
`Renderer.SetPropertyBlock(mpb, index)` 是**整块替换并拷贝到 Renderer**：同一 (Renderer, materialIndex) 上原来每个 RendererSet 条目各持一个 MPB，各自 Flush → 后写的把前一个条目设的属性整块抹掉。
`EFD_Enemy.asset` 条目2（`_HitColor` 受击）与条目3（`_DissolveValue` 死亡）都是 `material: 无` → 命中同一槽位 → 条目1 的 Switch 自发光（`_EmissionColor` 警戒白）会被受击闪白顺手抹掉。

### 方案（槽位为中心）
- 新类 `RendererSlot{Renderer, MaterialIndex, mpb, dirty}`：`SetColor(id,color)` 写块+置脏；`Flush()` 把整块 `SetPropertyBlock` 回渲染器（无写入/渲染器已销毁则跳过）。
- `RendererSet`：删掉自持 `mpb`、`List<(Renderer,int)>`、`ApplyToRenderers()`；改为 `List<RendererSlot> slots` + `AddSlot(slot)`；`ApplyColor` 只往各槽位 `SetColor`；`mpb.IsValid()` 的"有无匹配槽位"判断改成 `slots.Count > 0`。
- `EnemyControllerFX`：新增 `List<RendererSlot> slots`；`InitRS` 的外层循环正好每个 (renderer, materialIndex) 访问一次 → 在该层 `RendererSlot slot = null;` 惰性创建、命中条目共用同一个 slot 引用（**无需字典**）；`UpdateRS` 在**所有条目 Update 之后**统一 `Flush()` 各槽位。
- 槽位块跨帧保留 → 多个条目的属性天然共存（这既是修复点，也是新约束：条目必须在自己动画收尾写回"无效果值"）。

### 附带收益
一个槽位每帧最多一次 SetPropertyBlock（原来 N 个条目 N 次），且不再互删属性。

## 新增：战备配置界面 AirdropConfigWnd（战备购买 + 偏好）
### 数据层
- `02Data/AirdropData_SO.cs`：新增 `[Header("费用")] List<SKVP<OOPartEnum,int>> cost`（默认 1000 青辉石），与 `WeaponUpgradeData_SO.cost` 同款；`Editor/AirdropData_SOEditor.cs` 加了「费用」Section 并 `DrawField("cost")`。
- `02Data/ArchivesData_SO.cs` 空投区新增 `List<int> AirdropPreferList`（战备偏好 ID）与 4 个帮助方法：`IsAirdropBought/BuyAirdrop/IsAirdropPrefer/ToggleAirdropPrefer`。注意该 SO 的 `AirdropBuyDic` 在 .asset 里被写成**十六进制串**（非 Unity 默认 List<int> 格式），但实际存档走 `JsonUtility`，`List<int>` 正常序列化。

### UI
- 新文件 `Scripts/04UI/AirdropConfigWnd.cs`（Assembly-CSharp，与 `AirdropWnd.cs` 同目录）。左列表按 `AirdropType` 分组建表（组内偏好优先）、右上 RawImage + 独立 Camera + 运行时 `RenderTexture(1024)` 展示 `AirdropData_SO.creatObect` 模型（左键在显示区内左右拖动转模型，`RectTransformUtility.RectangleContainsScreenPoint` 判区域，Overlay 画布传 null 相机），右下价格（复用 `propertyManager.GetIcon/GetCount`）与 购买/已拥有 + 偏好切换按钮。
- 模型实例化后**关闭全部 MonoBehaviour、Collider，Rigidbody 置 kinematic**，并 `SetChildLayer(modelRoot.layer, true)`（同 ArmamentWnd 的展示模型做法）。
- 注册：`WndManager.airdropConfigWnd` 字段 + `Init()/Uninit()`（与 VehicleWnd 同款，需在窗口预制体的 UnityEvent 上挂 Init）；家具入口 `Furniture_General.furnData["SelectAirdrop"]` → `SetWndState(true)`。
- 购买走 `wndManager.CreatTip`（TipWnd 自带 `costs` 金额校验，不足时隐藏确认按钮），确认后扣 `propertyManager.SetCount` + `BuyAirdrop` + `Save()`；偏好同理即时 Save。

### ArmamentWnd
- `InitPlayerAirdrop` 排序改为 `OrderBy(偏好?0:1).ThenBy(ID)`，并 `SetActive(button.GetChild(1), arch.IsAirdropPrefer(id))` —— 预制体 `Resources/UI/Armament/ArmamentButton.prefab` 的第 1 个子物体（左上 12x12 黄色 Image）即"偏好标记"，子 0 = icon。

### [DisplayField] 用法确认
对 `AirdropConfigWnd` 中"代码运行时生成/赋值、不需要手动检视器配置"的字段（`_arch/_nowData/_firstData/_renderTexture/_model/_yaw/_dragging/_lastMousePosition`）加 `[SerializeField] + [DisplayField]`：编辑期隐藏、运行期只读显示。`_itemDatas`（`readonly Dictionary`）不可序列化，加了也无效果故未加。既有范例：`04UI/UI/Subtitle/SubtitleBase.cs` 的 `owner/target/targetPoint/root`（`Creat()` 内注入）。

## 环境调研：插件市场里与 Unity 相关的资源
- 市场路径 `C:\Users\admin\.codebuddy\plugins\marketplaces\`（`codebuddy-plugins-official` + `cb_teams_marketplace`）。
- 结论：**没有能直接"操控 Unity 编辑器"的 Unity MCP**（只有 `godot-mcp` 通过 WebSocket 驱动 Godot Editor）。
- Unity 相关可用物：① `external_plugins/game-development` 插件（`agents/unity-developer.md` 专家人设 + `skills/unity-ecs-patterns`）② `external_plugins/commands-game-development/commands/unity-project-setup.md`（生成工程目录/asmdef/.gitignore）③ `plugins/csharp-lsp`（C# LSP，对本项目改 C# 最有实用价值）④ `plugins/serena`（LSP 语义分析 MCP）⑤ `plugins/mcp-builder`（自建 MCP 的路径）。

## 接入 Unity MCP（CoplayDev/unity-mcp）
- 用户下载的 `C:\Users\admin\Downloads\unity-mcp-10.0.0.mcpb` 是 **Claude Desktop 的 MCP Bundle**（zip，仅含 `manifest.json`/`README.md`/`LICENSE`/logo，无 server 代码）。`manifest.json` 里的 `server.mcp_config` = `uvx --from mcpforunityserver mcp-for-unity`（Python，ephemeral）。CodeBuddy IDE **不支持 .mcpb 导入**（官方文档只有 MCP Market 一键装 + Add MCP 手写 JSON）。
- 已把该配置写入全局 `C:\Users\admin\.codebuddy\mcp.json`（`mcpServers.unity-mcp`，type stdio）。本机 **Python 3.12.10 有、uv/uvx 未安装**（必需前置）。
- Unity 侧还必须装包：Package Manager → Add from git URL `https://github.com/CoplayDev/unity-mcp.git?path=/MCPForUnity#beta`（或 `openupm add com.coplaydev.unity-mcp`），然后 `Window → MCP for Unity → Configure All Detected Clients`；Unity 需开着工程，MCP 才能连上编辑器。
- 该 MCP 暴露 40+ 工具：`manage_gameobject`/`manage_scene`/`manage_asset`/`manage_prefabs`/`manage_script`/`execute_code`(在编辑器里跑任意 C#)/`read_console`/`run_tests`/`manage_editor`(Play 模式/菜单项) 等 —— 即真正"操控 Unity 编辑器"的能力。

## 环境澄清 + unity-mcp 落地（本地嵌入包）
- **本机装的是正版 Unity 2022.3.62f3，不是团结引擎**：`D:\Unity Hub\Version\2022.3.62f3\Editor\Unity.exe`（ProductName=Unity、CompanyName=Unity Technologies、ProductVersion=`2022.3.62f3_96770f904ca7`，与 `ProjectSettings/ProjectVersion.txt` 完全一致）；C/D 盘无 Tuanjie.exe。（此前 MEMORY.md 里记的 `2022.3.62f2c1` 有误，实际是 f3。）unity-mcp 只要求 `unity >= 2021.3` → 版本满足。
- 本机代理：**Clash Verge Rev，混合端口 7897**（`%APPDATA%\io.github.clash-verge-rev.clash-verge-rev`）。实测 github 直连 / ghproxy.net / gh-proxy.com / jsdelivr 全部可达。
- 安装方式（绕开 git URL）：ghproxy 下载 `https://ghproxy.net/https://github.com/CoplayDev/unity-mcp/archive/refs/heads/beta.zip`（约 17MB）→ 取其中的 `MCPForUnity/` → 作为**本地嵌入包**复制到 `e:\Bluedivers\Packages\com.coplaydev.unity-mcp\`（716 文件，包版本 `10.2.1-beta.6`）。
- `Packages/manifest.json` 新增两行：`"com.coplaydev.unity-mcp": "file:com.coplaydev.unity-mcp"`、`"com.unity.nuget.newtonsoft-json": "3.0.2"`（原项目无 newtonsoft，需从 registry 拉；`com.unity.test-framework` 已有 1.1.33 ≥ 包要求的 1.1.31）。
- `mcp.json` 的 `env` 加了 `UV_DEFAULT_INDEX=https://pypi.tuna.tsinghua.edu.cn/simple`；实测 `uvx --from mcpforunityserver mcp-for-unity --help` 可正常拉取运行（uv 0.12.14，装在 `...\Python312\Scripts\uvx.exe`）。该 server CLI 支持 `--transport {stdio,http}`、`--http-url`、`--pidfile`、`--default-instance` 等。
- 撤销：删 `Packages/com.coplaydev.unity-mcp` + 去掉 manifest 那两行即可。

## 排查：unity-mcp「连不上 Unity 实例」
- 现象：CodeBuddy 能调用工具（`manage_scene`/`read_console` 都能发出），但一律返回 `No Unity Editor instances found`；`mcpforunity://editor/state` 字段全 null。
- 证据链：Unity 进程在跑；但 **6400（stdio 桥）/ 8080（HTTP 服务器）都没监听**，`~/.unity-mcp/` 无 `unity-mcp-status-*.json`，`Assets/UnityMCP/` 无日志 → Unity 侧桥**根本没启动**。
- **根因**：v10 默认走 **HTTP 传输**，而两个开关都是「默认不启动」——
  - `EditorConfigurationCache.UseHttpTransport = EditorPrefs.GetBool("MCPForUnity.UseHttpTransport", **true**)` → 默认 HTTP
  - `StdioBridgeHost.ShouldAutoStartBridge() => !useHttpTransport` → HTTP 模式下 **stdio 桥不自启**
  - `EditorPrefKeys.AutoStartOnLoad`（`MCPForUnity.AutoStartOnLoad`）默认 **false**，且**只对 HTTP 生效**
  → 默认配置下 Unity 侧什么都不起；而 CodeBuddy 的 mcp.json 是 **stdio** → 两头都对不上。
- 关键源码位置：`Packages/com.coplaydev.unity-mcp/Editor/Services/Transport/Transports/StdioBridgeHost.cs`（`[InitializeOnLoad]`，TCP `IPAddress.Loopback:6400`，握手 `WELCOME UNITY-MCP 1 FRAMING=1`，状态文件 `%USERPROFILE%\.unity-mcp\unity-mcp-status-{projectHash}.json`）、`Services/EditorConfigurationCache.cs`、`Helpers/HttpEndpointUtility.cs`（默认 `http://127.0.0.1:8080`）、`Constants/EditorPrefKeys.cs`。
- 两种修法：**A** Unity 窗口 `Window → MCP for Unity` → Connection 里把 Transport 切 **Stdio**（stdio 桥随即自启，CodeBuddy 配置不用动）；**B** 保持 HTTP → 窗口点 **Start** 或勾 Advanced 里的 Auto-Start on Editor Load → CodeBuddy 的 mcp.json 改成 HTTP 端点 `http://127.0.0.1:8080/mcp`。
- 诊断技巧：`Get-NetTCPConnection -State Listen` 查 6400/8080；`Test-Path "$env:USERPROFILE\.unity-mcp"` 判断 Unity 桥是否起过。

## ✅ unity-mcp 已连通（2026-09-15 15:45）
- 用户把 Unity 侧 Transport 切成 **Stdio** 后成功：`127.0.0.1:6400` 由 Unity PID 33364 监听，状态文件 `%USERPROFILE%\.unity-mcp\unity-mcp-status-3d9f2357.json`。
- 实例标识 **`Bluedivers@3d9f2357`**（多实例时可用 `--default-instance` 指定）。
- 实测通过：`mcpforunity://project/info` → `E:/Bluedivers` / Bluedivers / 2022.3.62f3 / StandaloneWindows64；`read_console(errors+warnings)` → **0 条**（说明装这个包没引入编译问题）；`manage_scene get_active` → `Assets/Scene/Utnapishitim.unity`（buildIndex 1，**当时 isDirty=true**）。
- 结论：CodeBuddy ⇄ `uvx mcp-for-unity`(stdio) ⇄ TCP 6400 ⇄ Unity Editor 全链路可用，`manage_gameobject`/`execute_code`/`manage_asset` 等 40+ 工具可用了。
- 注意：该 MCP 会**真实修改工程**（场景/prefab/脚本/资产），且 `execute_code` 可跑任意 C#；用前先 commit。

## 沉淀：新建 skill `.codebuddy/skills/unity-mcp/`
- 决策：**操作知识进 skill，记忆里只留指针**。理由：MEMORY.md 每轮都注入（越短越好），skill 按需加载不占常驻 token。
- skill 内容：前置条件（Transport=Stdio 的源码级原因）、三层连通性自检表、⚠️写操作先 commit、工具**只读/会写**分类清单、`batch_execute` 优先（上限 25）、5 个"最划算用途"、本项目 8 条避坑（SO 不能存实例引用 / DrawField / DisplayField 只对字段 / 新脚本 asmdef 归属 / 窗口注册范式 / 别动嵌入包 …）、常用调用范例、排障速查表、经 ghproxy 升级与卸载步骤。
- MEMORY.md 只加了 3 行「工具链：Unity MCP」指针，指向该 skill。

## 环境：安装 uv / uvx（Unity MCP 的前置依赖，已完成）
- 本机 Python 3.12.10（`C:\Users\admin\AppData\Local\Programs\Python\Python312`），原无 uv。
- `python -m pip install -U uv` 走默认清华源 **失败**（`SSLEOFError(8) UNEXPECTED_EOF_WHILE_READING`，max retries exceeded），改用官方源成功：
  `python -m pip install -U uv -i https://pypi.org/simple --timeout 60 --retries 3`
- 装到 `...\Python312\Scripts\`（已在 PATH）：`uv 0.12.14`、`uvx 0.12.14` 均可用。`uvx` 随 `uv` 一起提供，无需单独装。
- 意义：`mcp.json` 里 unity-mcp 的 `uvx --from mcpforunityserver mcp-for-unity` 现在具备运行条件。

## MCP for Unity 插件窗口汉化（Editor UI 全面中文化）
包路径：`e:\Bluedivers\Packages\com.coplaydev.unity-mcp\`（本地嵌入包，10.2.1-beta.6，改源码立即生效）

### 已汉化范围
- **UXML（11 个）**：`MCPForUnityEditorWindow.uxml`（6 个 tab：连接/工具/资源/生成/依赖/高级）、`MCPSetupWindow.uxml`、`EditorPrefs/{EditorPrefsWindow,EditorPrefItem}.uxml`、`Components/{Connection,Tools,Resources,Validation,AssetGen,ClientConfig,Advanced}/*.uxml`。
- **主窗口 C#**：`MCPForUnityEditorWindow.cs`（更新提示、可选依赖区 + Roslyn/ProBuilder/Cinemachine/VFX Graph/glTFast 行、安装/卸载/进度条/确认框）、`MCPSetupWindow.cs`（依赖检测、uv 安装、客户端选择步骤）、`EditorPrefsWindow.cs`。
- **分区 C#**：`McpConnectionSection`（状态/按钮/对话框/传输不匹配警告）、`McpClientConfigSection`（12 种状态显示、技能安装、路径浏览对话框）、`McpToolsSection`（分组名/标签/截图按钮/批量上限）、`McpResourcesSection`、`McpValidationSection`、`McpAdvancedSection`（全部 tooltip + 部署区）、`McpAssetGenSection`（服务商行、密钥状态、模型下拉）。
- **支撑层**：`Constants/HealthStatus.cs`（未知/健康/Ping 失败/不健康）、`Services/IClientConfigurationService.cs`（批量配置汇总）、`Clients/McpClientConfiguratorBase.cs`（配置/注销按钮、安装步骤、snippet 注释）、`Clients/Configurators/*`（23 个客户端的安装步骤全部中文化，含 Cherry Studio 手动配置说明）、`Dependencies/PlatformDetectors/{PlatformDetectorBase,WindowsPlatformDetector}.cs`（依赖检测详情 + Windows 安装建议）。

### 未汉化（有意保留）
- 工具/资源的 `Description` 元数据（由各工具类提供，量极大，且属"内容"而非"界面"）。
- `McpLog`/`Debug.Log` 控制台日志、URL、Prefs key、MCP 工具名/参数名、示例路径。
- macOS/Linux 平台检测器文本（本机为 Windows）。

### 风险与回滚
- 该包是 **untracked 本地嵌入包**，包更新（重新下载解压）会**覆盖**全部改动 → 更新前需备份 `Editor/Windows` 等目录；`git diff` 看不到（未纳入版本控制）。
- 汉化只改字符串字面量，未改逻辑；所有涉及 `name=`/`Q<T>("...")` 的查询键、`HealthStatus` 常量比较、`d.Name == "Python"` 之类依赖名匹配均未动。
- 修改后 lint 检查 0 error。

## 更正 + 补充：unity-mcp skill 最终落地情况
- **更正**：上面「沉淀」一节写的「MEMORY.md 只加了 3 行『工具链：Unity MCP』指针」已过期。MEMORY.md 已被另一会话大幅精简重写，且已含「## MCP for Unity 插件（本地嵌入包）」一节（记录了窗口汉化）。最终做法改为**在该节尾部追加 2 行**：客户端配置路径 + 桥端口/发现文件/实例 `Bluedivers@3d9f2357` + 指向 skill 的指针。
- **更正（重要）**：skill 里的「升级 unity-mcp」步骤原本直接覆盖 `Packages/com.coplaydev.unity-mcp/`，**会冲掉本日做的整套窗口汉化**（该包 untracked，`git diff` 看不见）。已在 `SKILL.md` 补醒目升级前警告（先备份 / `git add -f` 提交 `Editor/` → 覆盖 → 回灌汉化 diff），并把「本项目避坑」里"别动嵌入包"那条改成"中文文案是有意为之，别当脏数据清理"。
- 确认 `mcp.json` 的 `UV_DEFAULT_INDEX` 仍指向清华源，uvx 实测可用 —— **pip 走清华源 SSL 失败 ≠ uv 失败**，两者不冲突。
- skill 触发依赖 frontmatter 的 `description`（已写明：核对 prefab/Inspector、批量校验 SO、Play 模式截图、读 Console、排查 MCP 连接问题）。
