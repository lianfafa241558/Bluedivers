# 2026-09-20

## 动态撤离任务（MissionEvacuateMobile）+ 凯伊带队撤离（SpecUnitKei）
仿照 `MissionEvacuateStatic` 补完两个半成品文件，共动 4 个文件：

- **新增** `Assets/Scripts/02Game/05Interactable/Furniture_ReturnBag.cs`（`FPSGame.Furn`）
  - 挂在凯伊「回收包」子物体上的简单家具；`Available`（static，由任务控制）＋`enabled`（由凯伊取出）双闸门
  - 非 Actor 物体上挂家具，**必须 override `ShowName`/`Id`/`Icon`**，否则 `Furniture_Attached` 里 `_actor.ShowName` / `_actor.Portrait` 空引用
  - `Operate()` → `BattleEventSub.ReturnBagOperate(owner)`
- **改** `01Manager/Battle/BattleEventSub.cs`：加 `OnReturnBagShow`(凯伊取出信标) 与 `OnReturnBagOperate`(玩家请求撤离) 两个事件，避免任务与家具直接耦合
- **改** `02Game/Game/Mission/Evacuate/MissionEvacuateMobile.cs`
  - 阶段 `Activation(等凯伊取包/等玩家交互) → Approach(300s 限时，运输船从 500m 降到悬停高度) → Land(等待登机，MedivacController.EvacuateTick 全员在箱内即 Complete) → End`
  - 请求撤离时：`BattleEventSub.Evacuate(new(areaPoint))`（凯伊带队 + `PatrolContriller` 收拢巡逻队）、玩家与撤离点中点刷 `WaveCreateParams.Extra.Scale(m_WaveScale)`、`CreatNotice("Ayane","CountDownBegins")`、`AudioSvc.PlayMusic(MusicGroup.Evacuate)`
  - 超时 → 运输机 `Play("Evacuate")` 起飞并 `FailMission()`；成功 → `CompleteMission()` + `GameRoot.GameState = Transition`
  - `Uninit()` 复位 `Furniture_ReturnBag.Available` 并退订事件（`MissionBase.OnDestroy` 会调虚方法 `Uninit`）
  - 运输机位移用 `GameRoot.CreatePerTimer`（**每帧回调**持续 N 秒，到点回调 `Landing`），闭包里加了 `stage`/`medivac` 守卫，避免任务提前结束仍回调
- **改** `02Game/AI/StateMachine/SpecUnitKei.cs`
  - `Start` 里 `InitReturnBag()`：`TryGetComponent` 否则 `AddComponent<Furniture_ReturnBag>()` 运行时补挂（**Kei.prefab 上目前没有该组件**），随后 `enabled = false`（OnDisable → 移出 `Furniture_Attached.list`）
  - 抵达呼叫点（`OnCall` 的到达分支）→ `ShowReturnBag()`：激活回收包 + 启用家具 + `BattleEventSub.ReturnBagShow`
  - 订阅 `BattleEventSub.OnEvacuate` → `OnEvacuate()` 进入 `m_Guiding` 带队模式；`Move` 分支每 **5m（水平距离）** 在射线落点放一根 `returnPoint`（`VFXManager.Creat`，ReturnKeiFlag 自带 `LimitedLife LiftTime=60` 会自回收）
  - 带队途中 `OnCall` 直接 return（不打断引导）；`ArriveTarget()` 统一收尾（停声/表情/释放呼叫旗/救人/取信标）
  - 抽出 `TrySpeech(SoundGroup_SO)`（原 `OnCall` 里 `lastSpeechTime`/`speechShowTime` 那段的复用）

## 验证
- 经 MCP（实例 `Bluedivers@4a3e6a7b`，projectRoot 已核对 `D:/Pro/Bluedivers`）：`validate_script level=standard` 三个文件 **0 error**（warning 为启发式）；`read_console` 无编译错误；`execute_code` 遍历 `AppDomain` 反射确认三个类型都在 `Assembly-CSharp`、新事件/新方法/`Available` 属性均存在、`m_EvacuateTime` 默认 300

## 待办（已告知用户，未擅自改 prefab）
- 建议把 `Furniture_ReturnBag` 正式挂到 `Assets/Resources/Prefabs/BattleBase/Kei.prefab` 的 `KeiReturn` 子物体上（该物体已有 BoxCollider），再把 `SpecUnitKei.Start` 里的运行时 `AddComponent` 兜底去掉；当前靠运行时补挂也能正常工作

## 【修正】改用实例引用 + 实例事件，去掉 BattleEventSub 新增项
用户指出「不要什么都往 BattleEventSub 里面塞，完全可以在 `MissionEvacuateBase.cs:73` 获取组件然后监听」。已改为：

- **撤销** `01Manager/Battle/BattleEventSub.cs` 里我新增的 `OnReturnBagShow`/`OnReturnBagOperate`（保留原有 `OnEvacuate`——它是**多订阅者广播**：凯伊带队 + `PatrolContriller` 巡逻队收缩）
- `02Game/Game/Mission/Evacuate/MissionEvacuateBase.cs`：`using FPSGame.AI;`；新增 `protected SpecUnitKei kei;`；第 73 行改成 `var keiGo = ResSvc.Instance.CreatPrefab("Prefabs/BattleBase/Kei", ...); if (keiGo) kei = keiGo.GetComponent<SpecUnitKei>();`
- `SpecUnitKei`：新增 `public event Action OnReturnBagShow;`（本地事件，`ShowReturnBag()` 里 invoke）与 `public Furniture_ReturnBag ReturnBag`（getter 内 `InitReturnBag()` 惰性补建，避免任务在 `Start` 之前取用）；`InitReturnBag` 加 `if (!returnBag || m_ReturnBag) return;` 早退，防止重复调用把已启用的家具又 disable 掉
- `Furniture_ReturnBag`：`static Available` → **实例** `Usable`（不再跨实例漏状态）；`BattleEventSub.ReturnBagOperate(owner)` → 自有 `public event Action<GameObject> OnRequestEvacuate`（把 `owner` 带出来，因为 `Furniture_Attached.owner` 是 protected）
- `MissionEvacuateMobile`：`Activation` 里 `kei.OnReturnBagShow += ...` + `kei.ReturnBag.Usable = true` + `bag.OnRequestEvacuate += OnReturnBagOperate`；`Uninit` 反向退订并 `Usable = false`；`OnReturnBagShow()` 不再带参数
- 验证：4 个文件 `validate_script` 0 error；`read_console` 0 error；`execute_code` 反射确认新成员在 `Assembly-CSharp`、两个 BattleEventSub 新事件与 static `Available` **已消失**、`MissionEvacuateBase.kei` 字段类型为 `SpecUnitKei`

经验：**能拿到实例就别发全局事件**；家具类的"谁来用我"用实例事件（`Furniture_Attached.OnOperate` 无参数，拿不到 user，需自定义 `Action<GameObject>`）；跨实例开关用实例字段而非 `static`。

## 【追加】IsFast 快速模式跳过等待交互
`MissionEvacuateMobile.Activation` 里加分支：`IsFast` 为真时**不订阅**回收信标（`bag.Usable` 保持 false，玩家也没有交互提示），直接 `OnStartEvacuate(null)` 进 `Approach` 阶段，然后 return。

- `OnStartEvacuate(GameObject user)` 的 `user` 允许为 null（快速模式没有点击信标的玩家），`GlobalEventSub.PlayMeetSpeech(user, ...)` 前加 `if (user)` 守卫——`PlayMeetSpeech` 的订阅方会去 user 上取 `PlayerSpeechManager`，传 null 会空引用
- 与静态撤离的 `IsFast` 语义一致（那边是 `keyScreen.SetStage(procedure.Count - 1)` 直接跳到终端最后一步）
- 注意 `IsFast` 还会影响基类 `InitMission`：会改成找 `Tag=StartPoint` 的物体当 `entity`，所以快速模式下 `areaPoint`＝起始点，运输机落在起始点属于预期

## 【追加】动态撤离不做 StartPoint 顶替
用户要求：Mobile 的 `entity` 不换成场景中 `Tag=StartPoint` 的物体。做法（不动静态撤离的既有行为）：

- `MissionEvacuateBase`：新增开关 `protected virtual bool UseSceneStartPoint => true;`，`InitMission` 里条件改成 `if (IsFast && UseSceneStartPoint)`
- `MissionEvacuateMobile`：`protected override bool UseSceneStartPoint => false;` → 快速模式也走 `base.InitMission()`（用场景实体或实例化 prefabs），`areaPoint` 仍是任务实体所在撤离点
- 顺带在 `pos = entity.Pos;` **之前**补了 `if (!entity) { Debug.LogError(...); return; }`：原先 entity 为空会直接 NRE，而 Mobile 关掉顶替后这条路径更容易走到（无场景实体且 `prefabs` 为空时）
- 验证：两文件 `validate_script` 0 error；`read_console` 0 error；反射确认 `UseSceneStartPoint` 在基类为 virtual、Mobile 覆写值为 `false`

## 【追加】撤离期间凯伊不响应呼叫
原先只在 `m_Guiding`（走向撤离点途中）拦呼叫，**抵达撤离点后 `m_Guiding` 置 false，玩家再叫凯伊就会把他叫离撤离点**。改为：

- `SpecUnitKei` 新增 `bool m_Evacuating`（撤离已开始，单向不复位；凯伊本身按任务实例创建，下次任务自然是新的）
- `OnCall` 开头守卫改成 `if (m_Evacuating) return;`（覆盖"带队途中"+"在撤离点等登机"两段）
- `OnEvacuate` 里 `m_Evacuating = true; m_Guiding = true;`；`ArriveEvacuatePoint()` 只清 `m_Guiding`，`m_Evacuating` 保持
- 验证：`validate_script` 0 error；反射确认 `m_Evacuating` 已在 `Assembly-CSharp`。控制台里另有两条既有报错与本次无关：`EnemyFXControllerUnit.OnAttack` 里 `Debug.LogError("触发攻击触发器")`、`DrawLabelUtils.OnDrawGizmos` 的 NRE

## 【追加】动态撤离增援改为「逐玩家追击+无限续刷」
需求：原来是"在玩家与撤离点中点刷一波"，改成**降低 Scale、为每名玩家各刷一波、目标点=玩家位置（追着打），且该波清空后自动续刷（任务结束才停）**。

- **`01Manager/Battle/BattleManager.cs`**：`WaveCreateParams` 加可选字段 `System.Action onEnd`（波次结束回调），默认 null，不影响既有调用方
- **`01Manager/Battle/RobotWave.cs` / `ZergWave.cs`**：各加 `System.Action onEnd` 字段，ctor 里 `onEnd = param.onEnd;`，在 `Dispose()` 末尾 `IsDisposed=true` 之后取局部变量并 `Invoke()`（`Dispose` 有 `IsDisposed` 早退保证只调一次）。⚠ 回调里 `CreatWave` 会在 `WaveManager.Update` 遍历 `ticks` 时 `ticks.Add`，因循环是 `for(i=Count-1;i>=0;--i)` 倒序，新增项在末尾不会被本帧访问，安全
- **`02Game/Game/Mission/Evacuate/MissionEvacuateMobile.cs`**：
  - `m_WaveScale` 字段名改「每名玩家波次倍率」，默认 0.5→**0.25**，tooltip 改
  - 新增 `private bool m_ChaseReinforce;`（非序列化）
  - `CreatReinforcement()` 改为：`m_ChaseReinforce=true;` + `foreach(var player in ActorsManager.Players) CreatChaseWave(player);`
  - 新增 `CreatChaseWave(I_Actor player)`：`!m_ChaseReinforce || !player.IsValidMono() || player.ActorState==Core.ActorState.Dead` 直接 return；否则 `WaveCreateParams.Evacuate.Set(player.Pos).Scale(m_WaveScale)`，`param.onEnd = () => CreatChaseWave(player);`（**每名玩家一条独立续刷链**），再 `BattleManager.Instance.CreatWave(param)`
  - `m_ChaseReinforce=false` 在 `Uninit()`、`TimeOut()`、`End()` 三处，保证任务结束/超时/卸载后不再续刷
- **prefab**：`Assets/Resources/Prefabs/Mission/Iogin/EvacuateMobile.prefab` 与 `EvacuateMobileFast.prefab` 的 `m_WaveScale: 0.5` → `0.25`
- 说明：追击是"每波以玩家当时位置为 center、range=60 外圈刷新并走向 center"，旧波清空才刷下一波，故为**近似追击**；要真正跟随需逐单位改 `HomePoint`，本次未做
- 验证：4 个脚本 `read_lints` 0 诊断（未跑 MCP `validate_script`）

## 【追加】追击增援：center 持续跟踪 + 倒地暂停/复活恢复
用户追加两点需求，已实现：

- **`WaveCreateParams` 加 `System.Func<Vector3> centerGetter`**：不为 null 时波次每 Tick(1s) `center = centerGetter()`。
  - `RobotWave.ZergWave` 各加字段并在 Tick 开头刷新；`Dispose` 里置 null
  - ⚠ 只影响**尚未落点**的单位：RobotWave 的鹰在移动 center 周围刷新、空投后单位走向最新 center(跟随好)；ZergWave 的 `points` 生成环在 ctor 固定，仅 Ongoing 里新刷出单位的 `HomePoint = center+rand` 跟随 → 已有单位不重新寻路
- **倒地暂停、复活恢复**（`MissionEvacuateMobile`）：
  - 新增 `private readonly List<I_Actor> m_PausedChase`（非序列化）
  - `CreatChaseWave(player)`：`!IsValidMono()`→从暂停表移除并 return；`ActorState==Dead`→加入 `m_PausedChase` 并 return(不刷)；否则正常建波(`centerGetter=()=>player.Pos` 带 last 兜底、`onEnd=()=>CreatChaseWave(player)`）
  - `ResumePausedChase()` 在 `Tick()` 里每秒调用：暂停表里对象销毁→移除；仍 Dead→continue；已复活(`Actor.OnRevive` 会把 `ActorState` 置回 Normal)→移除并重建波
  - 新增 `StopChaseReinforcement()`（`m_ChaseReinforce=false`+清空暂停表），在 `Uninit`/`TimeOut`/`End` 复用
- 复活检测走 Tick 轮询（1s 粒度）而非订阅 `BattleEventSub.PlayerRevive`/`Health.OnRevive`：任务本身已有 Tick，避免订阅生命周期问题
- 若要**已有单位**也实时重新寻路追人，需周期性(如每几秒)重设存活单位的 `HomePoint`（本次未做，`SetNavDestination` 频繁调用有寻路开销）
- 验证：4 个脚本 `read_lints` 0 诊断

## 【追加】ZergWave 空投点跟随重部署
“centerGetter 模式下，实际中心离初始中心太远就回收当前空投舱、以当前中心为新基准重算生成环并重新空投”（默认情况——无 centerGetter——完全不变）。只改 `01Manager/Battle/ZergWave.cs`：

- 新增 `const float RedeployDistance = 50f;`（放类顶部，遵规范）；字段 `anchorCenter`(生成环基准中心)/`float range`(存 param.range 供重算)/`bool useCenterPoints`(= param.points==null)
- ctor：`anchorCenter = center; range = param.range;`；`param.points==null` 分支的生成环代码抽成 `ResetPoints()` 复用（随机调用序列不变，行为等价）
- `Tick()` 里 `center = centerGetter()` 之后判断：`useCenterPoints && state==Ongoing && !completeCreat && Vector3.Distance(center, anchorCenter) > RedeployDistance` → `RedeployPods()`
- `RedeployPods()`：`EndCreatObjects()`(回收当前空投舱) → `anchorCenter = center` → `ResetPoints()` → 以 `waveUseObject[0]` 在新 points 重新空投(`ResetLift(51)`)、加入 `creatObject`
- `EndCreatObjects()`：对 `creatObject` 每项 `GetComponentInChildren<Animator>().Play("End")` + `LimitedLife.ResetLift(6)`（**这是本文件既有的回收写法**，走 LimitedLife 让 VFXManager 池自行回收），再 Clear；`Ongoing` 里原先内联的 completeCreat 收尾循环也改为复用它
- ⚠ 只覆盖随机生成点模式；`param.points` 显式给定的场景不重部署。回收是"播放 End + 6 秒后回收"，不是瞬间消失（要瞬删可改 `VFXManager.Release(go)` → 置 `allowRelease=true`）。重部署发生在 switch 之前，故本 Tick 的 Ongoing 就会在新点生成单位
- RobotWave 未改：其凤凰鹰本就在移动 center 周围刷新，已能跟随
- 验证：`read_lints` 0 诊断

## 【追加】凯伊回收标记朝向
`SpecUnitKei.DropReturnPoint()` 里 `VFXManager.Creat(returnPoint, point)` 改为 `VFXManager.Creat(returnPoint, point, transform.rotation)`，让回收标记朝向与凯伊当时一致。
- 依据：`VFXManager.Creat(tmp, pos, rotation, parent)` 第三个参数就是旋转(内部 `ps.transform.rotation = roation;`)，原先不传即用 `default`=`Quaternion(0,0,0,0)` 退化四元数
- 传的是完整 `transform.rotation`(含 pitch/roll 分量)；若标记是贴地物、只想要水平朝向，可换成 `Quaternion.Euler(0, transform.eulerAngles.y, 0)`

## 【修正】上轮"基类 InitMission 建了孤儿"的判断作废
- **Kei**：是带独立行为的友方 NPC，基类创建是对的，不需要给 Static 开"可不建"的口子
- **基类那台 `Land` 态运输机不是泄漏**：`MedivacController.LandTick`(167-180) 在舱内无人(`count==0`)时先 `nextComplete=true`，下一 Tick 就 `complete=true; anim.Play("Evacuate"); Destroy(gameObject,14)` **自回收**；子类再 `CreatEvacuateMedivac` 覆盖 `medivac` 字段属设计如此（重新赋引用，不是"孤儿"）
- 唯一残留疑点：`MissionEvacuateStatic.cs:202` 悬停目标写成 `(Vector3.up*50)*lossyScale.x`，**没加 areaPoint**（而创建位置 195 是 `areaPoint+up*500`，是相对的）；Mobile 同步骤是 `areaPoint+up*(m_HoverHeight*scale)`。两台船"悬停点"不一致——待用户确认是否有意
- 可上移清单不变：`End` 主体 / End 播报 / 运输机创建样板 / `m_EvacuateTime` / `BeginEvacuateCountdown` / `LandMedivac` / `ApproachHover` / `ApplyFastMode` / `AreaHavePlayer`

## 【追加】动态撤离收尾与静态撤离统一 + Medivac 强制起飞
需求：Mobile 的最后收尾效果参照 Static——**不论成功(全员登船)还是超时失败**都走 `MissionEvacuateStatic.End()`(253-260: 起飞→Transition→`EndGame(14)`) 然后 `case EvacuateState.End`(128-133: 6 秒后按 `IsComplete` 播 "End"/"Fail")；因运输机要"满员才起飞"，需在 `MedivacController` 加外部强制起飞且**只隐藏已登船玩家**。

- **`Effect/MedivacController.cs`**：
  - `Tick()` 里原内联的 BoxCollider 局部空间判定抽成 `private bool IsInBox(Vector3 worldPos)`
  - 原 `EvacuateTick` 的起飞体抽成 `private void TakeOff()`：`complete=true` + `Play("Evacuate")` + 开 `cam` + `Complete?.Invoke()` + **只对 `IsInBox` 的玩家** 设 parent=target 并 SetActive(false)（原先无条件隐藏全部——全登船时等价）
  - 新增 `public void ForceTakeOff()`：`if(complete) return; TakeOff();`（不等全员）
- **`02Game/Game/Mission/Evacuate/MissionEvacuateMobile.cs`**：
  - `Tick()` 新增 `case EvacuateState.End: if (--countDown == 0) CreatNotice("Yuuka", IsComplete ? "End" : "Fail");`
  - `End()` 改为统一收尾(照抄 Static)：`stage=End; StopChaseReinforcement(); countDown=6; CreatNotice("Ayane","TakeOff"); GameState=Transition; BattleManager.Instance.EndGame(14);`（删掉了原来的 `UpdateText("撤离完成")`）
  - `TimeOut()` 不再自己收尾，改为 `if(!medivac) End(); else medivac.ForceTakeOff();`——强制起飞后 `Complete` 回调仍走同一个 `End()`
  - `CreatMedivac` 的 `medivac.Complete += End` 不变，两条路径自然汇合
- 注意语义：`IsComplete`(=父任务是否完成)决定最终 "End"/"Fail"，并非"是否及时登船"；与 Static 一致
- 验证：两文件 `read_lints` 0 诊断

## 【追加】地形树：既无碰撞也不可摧毁 → 落地 A 方案（TreeDestructor）

诊断（纯读文件；本轮无 MCP 工具可用，二进制 `MainMap.asset` 读不到）：
- 草=Terrain **Details**（`SetDetailLayer`）**永远没有碰撞体**（Unity 只为 Trees 生成）；树的碰撞体=树原型预制体自带 Collider + `TerrainCollider.m_EnableTreeColliders`（`TestScene.unity` 中 MapRoot 的 TerrainCollider = 1），且**只在运行时生成**，编辑模式 Physics Debug 看不到
- 本项目树/草都由 `GenerateNoiseTerrain` 运行时写入（`SetTreeInstances(..., snapToHeightmap: true)` / `SetDetailLayer`）；`Assets/Art/Nature/Tree/Prefab/tree_a..k.prefab` 与 `Assets/Tree.prefab` **均无 Collider**（grep `Collider` 0 匹配）
- NavMesh：MapRoot 的 `NavMeshSurface` `m_UseGeometry: 0`(RenderMeshes) + LayerMask bits 8 → 烘焙不含树，AI 直接穿树
- `TerrainData` **没有 `RemoveTreeInstance`**（只有 `SetTreeInstance(s)`/`GetTreeInstance`/`treeInstanceCount`）；全仓无任何删树/改树调用
- 爆炸弹坑只改高度图（`ModifyTerrain`/`FpsHelper.Hit`），不会动树（树原地不动，甚至浮空/被埋）

实现：
- **新增** `Assets/Scripts/08Map/TreeDestructor.cs`（`FpsGame.MapUtils`，与 `GenerateNoiseTerrain` 同 asmdef=FpsGame.MapUtils，`autoReferenced: true` 故 Assembly-CSharp 可调）：
  - `Rebuild(Terrain)` 建"树索引 → 世界坐标/高度/半径"表；原型尺寸读 `treePrototypes[i].prefab` 的 Renderer 包围盒，失败退回默认 8m 高 / 0.5m 半径
  - 销毁 = 把实例 `heightScale/widthScale` 置 0（**绝不能改 `position`/`prototypeIndex`，否则 `SetTreeInstance` 抛 `ArgumentException`**）→ 索引不变、可 `RestoreAll` 还原
  - 提交：`SetTreeInstances(_instances, false)` 整表一次 + `terrain.Flush()`；`LateUpdate` 合并提交、`_maxCommitPerSecond = 4` 限流（**snapToHeightmap 必须传 false**，否则弹坑地形会把全体树重新贴地）
  - API：`DestroyInRadius(center, radius, yTolerance)` / `RaycastTree(ray, maxDist, out hit)`（圆柱近似：XZ 圆 + Y 高度段，树无碰撞体必须自研）/ `DestroyAll` / `RestoreAll` / `GetAliveTreePositions`；`OnDrawGizmosSelected` 画存活树球
- **接入 3 处**：`GenerateNoiseTerrain.ApplyFractalNoiseToTerrain` 在 `SetTreeInstances` 后 `TreeDestructor.Rebuild(terrain)`（自动 AddComponent 到 Terrain 物体，无需改场景/prefab）；`00Tools/FpsHelper.cs` 地形破坏分支 `destructe > 0` 里 `TreeDestructor.DestroyInRadius(point, destructe.RawFloat)`（加 `using FpsGame.MapUtils;`）；`Scripts/Effect/ModifyTerrain.cs` 弹坑循环里同半径销毁
- 树数量很小（`treeProbability * cells / 10000`：雨林 1025² 约 26 棵）→ 线性扫描足够，不需要空间网格
- 4 个文件 `read_lints` 0 诊断（未跑 Unity 编译/MCP 验证）
- 待实测：Play 模式下"缩放置 0"是否真的完全不可见；若仍有残留，把提交改成"整表剔除该实例"（索引会重排，需重建表）

## 【追加】TreeDestructor 增加"树种白名单"：只允许指定树种被爆炸摧毁
需求：5 种树里只让其中 2 种可被炸掉。按 `TreeInstance.prototypeIndex`(= `TerrainData.treePrototypes` 下标，**0 基**)做白名单。

- `08Map/TreeDestructor.cs`：
  - 新增 `HashSet<int> _destructiblePrototypes`(**空集合 = 全放行**，保持向后兼容) + 每树预计算 `bool[] _destructible` / `int[] _treePrototype`（爆炸时只查数组，不查集合）
  - 静态 API：`Rebuild(Terrain, IList<int> whitelist = null)`、`SetDestructiblePrototypes(params int[])`(运行时可改，只重算标记不重建地形)、`IsPrototypeDestructible(int)`；`RaycastTree(..., bool onlyDestructible = true)`（默认跳过免疫树）、`DestroyAll(bool includeProtected = false)`
  - `DestroyInRadiusInternal` 里加 `if (!_destructible[i]) continue;` → **所有来源**（`FpsHelper.Hit` 爆炸 + `ModifyTerrain` 弹坑）统一受白名单约束
  - Inspector 新增只读字符串 `树种统计`（形如 `[0]x12 免疫  [3]x5 可毁`），用于对照 `索引 ↔ 数量 ↔ 可毁/免疫`（运行时显示）
- 配置落点：`GenerateNoiseTerrain` 新增 `[SerializeField] private List<int> _destructibleTreePrototypes`（Foldout「树」下，0 基索引，留空=全部可摧毁），生成时传 `TreeDestructor.Rebuild(terrain, _destructibleTreePrototypes)`
- 用户例子「5 种树，1-3 不能、4-5 可以」→ 按 0 基填 `3, 4`
- 注意：`TreeDestructor` 是运行时 AddComponent，其序列化字段不落盘 → **配置必须放在场景里已存在的 `GenerateNoiseTerrain` 上**
- 验证：两文件 `read_lints` 0 诊断（未在 Unity 编译/Play 验证）

## 【追加】"完全没长树"排查：是数量公式 + 小图，不是 TreeDestructor 的锅
用户反馈 TestScene 刚生成完"没有树但草正常"。查文件得：
- `TestScene.unity` 里 `GenerateNoiseTerrain` 的序列化状态是 `width/height = 513`、`size = 512`（Small 图）、`_overridePreset: 0`（走预设）、`_destructibleTreePrototypes:` 为空行（=空列表=全放行）→ **白名单/销毁逻辑无嫌疑**（且销毁只在爆炸/弹坑时触发）
- 树公式 `targetCount = FloorToInt(prob × 513×513 / 10000) = FloorToInt(prob × 26.32)`：沙漠/平原 0.03→**0**、山地 0.02→**0**、高原 0.08→2、盆地 0.10→2、丘陵 0.12→3、雨林 0.25→6；再加坡度/高度约束 → 2~6 棵里还可能更少。**半个地形类型在 512 图上必然 0 棵**
- 草：`SpawnDetails` 按 detail 格子 `Random.value < detailDensity` 逐格判定，与"目标总数"无关 → 512 图约 26 万格，0.02 也能出上千个草点
- 场景里 `treeProbability: 20` 只在 `_overridePreset=1` 时生效（语义≈万分之一个格子 → 20 约 526 棵）；以前看到树大概是因为当时勾了覆盖
- 已在 `SpawnTrees` 加三条日志：生成 `X/Y 棵（格子/概率/原型数）`、目标为 0 时给算式警告、`Tree Prototypes` 为空时警告
- 未定的修法（待用户选）：① 勾覆盖预设 + `treeProbability≈20` 快速验证 ② 把公式改成"按面积密度(棵/公顷) + RoundToInt + 下限" ③ 每个地形类型直接配"每棵树覆盖多少平米"

## 【落地】树生成改成与草一致的逐格概率算法
用户选"和草一样"。改 `08Map/GenerateNoiseTerrain.cs`：
- `SpawnTrees` 重写：遍历 `(width-2)×(height-2)` 每格，`if (Random.value >= treeRate) continue;` **先掷骰**、命中后才查高度/坡度约束（约束只对极少数格子计算，比草更快）；按 `maxTimePerFrame` 每 8 行让出一次；prototypeRange 用 `Mathf.Clamp` 防原型数不足；结尾打印 `[树] 生成 N 棵（WxH 格，每格概率 p，约每 1/p ㎡ 一棵，地图 X㎡，原型 N 个）`
- 语义统一：`treeProbability`（预设字段 + 组件覆盖字段）= **每格(=每㎡)出现概率**，`0.001 ≈ 每 1000㎡ 一棵`，与地图尺寸无关；`[InspectorName]` 已标注；组件字段默认 0.1 → 0.001
- 7 套预设重新标定（旧值 0.02~0.25 是"字面百分比"语义，当逐格概率会爆到几万棵）：Desert 0.0002 / Mountains 0.0003 / Plains 0.0006 / Plateau 0.0008 / Basin 0.001 / Hills 0.0015 / Rainforest 0.003 → 512 图约 52/78/157/209/261/392/783 棵（约束后再打折）
- `TestScene.unity` 里 `treeProbability: 20` → `0.001`（避免以后勾"覆盖预设"时爆量）
- 注意：实际棵数 = 名义值 × 通过约束的格子比例，所以坡度/高度限制严的地形（山地 heightMax 0.4、沙漠 heightMax 0.7）会明显低于上表
- 验证：`read_lints` 0 诊断（未在 Unity 编译/Play 验证）

## 【落地】石块与树分开生成（关键前提：0-6 是石块，7-10 才是真树）
用户指出地形 Tree Prototypes 里 **0-6 = 石块、7-10 = 真树**（之前所有预设的 `treePrototypeRange` 写的是 (0,1)/(0,3)/(0,5) 之类，等于**全在生成石块**）。

- 新增 `[Serializable] struct VegetationSpawnData { probability(每格概率)/prototypeRange(含头含尾)/minSlope/maxSlope/minHeight/maxHeight }`，`TerrainPresetData` 里原来那 6 个 `tree*` 字段替换为 `rockSpawn` + `treeSpawn` 两份
- 生成方法 `SpawnTrees(preset)` → `SpawnVegetation(VegetationSpawnData cfg, string label, float rateOverride)`，逐格概率算法不变；调用点改为连调两次：`preset.rockSpawn`("石块") + `preset.treeSpawn`("树")，最后一次性 `SetTreeInstances`
- 7 套预设都重配了两份参数（石块 range (0,6) 且约束更宽：坡度 35~60、高度范围宽；树 range (7,10) 沿用原有坡度/高度）。概率：沙漠 石 0.0012/树 0.0002、高原 0.0015/0.0008、雨林 0.0008/0.003、丘陵 0.0025/0.0015、盆地 0.002/0.001、平原 0.0012/0.0006、山地 0.0025/0.0003
- 覆盖模式改成"每类各自可覆盖"：组件新增 `_rockProbability`（默认 -1），`treeProbability` 也改为默认 -1（**<0 = 沿用预设**），`TestScene` 里对应行同步改成 -1
- 原型不足时 `Mathf.Clamp` 收窄并打警告；日志改成 `[植被] 石块 生成 N 处（原型 0~6，每格概率 …）`
- `_destructibleTreePrototypes` 标签改为"可被摧毁的索引（0基，0-6石块/7-10树，留空=全部可摧毁）"——想"树能炸掉、石块炸不掉"就填 7,8,9,10
- 验证：`read_lints` 0 诊断，且全仓已无 `treePrototypeRange/treeMin*/treeMax*/SpawnTrees` 残留引用（未在 Unity 编译验证）

### 【修正】CS1061：漏改"使用地形预设"日志
用户拿到 `GenerateNoiseTerrain.cs(533,85): error CS1061: TerrainPresetData 未包含 treeProbability`——是 `ApplyFractalNoiseToTerrain` 里那行 `Debug.Log($"使用地形预设: {terrainType} | 噪声层数={preset.octaves} | 树概率={preset.treeProbability}")` 还在引用被删掉的旧字段。已改为同时打印 `preset.rockSpawn.probability(原型…)` 与 `preset.treeSpawn.probability(原型…)`。
教训：**替换结构体字段后，除了初始化处，日志/注释/调试代码里也会引用旧字段名**，改完必须全仓搜一遍旧名（本次 `read_lints` 在 IDE 未重新编译时不一定报出来，用户编译才暴露）。

## 【落地】ModifyTerrain 的弹坑与附加地形都清除区域内的树（7-10）
需求：`ModifyTerrain` 的 `datas`（弹坑）和 `additionTerrain`（附加地形）都要清掉区域内的树（原型 7-10），**石块 0-6 保留**。

- `TreeDestructor` 把入队逻辑抽成统一核心，新增**两种语义**：
  - 摧毁：`QueueInRadius(..., protoMin=-1, protoMax=-1)` → 走"可被摧毁"白名单（爆炸伤害路径，行为不变）
  - 清除：指定原型区间 → **忽略白名单**，`public static int ClearInRadius(center, radius, protoMin, protoMax, yTolerance = 0)` 与 `public static int ClearInRectXZ(center, halfSize, protoMin, protoMax, yTolerance = 0)`
  - 公共判定抽成 `IsQueueCandidate(i, protoMin, protoMax)` + `QueueTree(i)`；`DestroyInRadiusInternal` 现在只是 `QueueInRadius(center, radius, yTol, -1, -1)` 的转发
- `ModifyTerrain`：
  - 新增 `[SerializeField] Vector2Int clearVegetationRange = (7, 10)`（Inspector 可改）
  - `datas` 循环：`TreeDestructor.ClearInRadius(pos, data.outerRadius, range.x, range.y)`
  - `additionTerrain` 分支：附加地形是"角点=position、XZ=size"的方块且未被旋转 → 用**轴对齐矩形**精确清除：中心 = `additionTerrain.transform.position + (size.x/2, 0, size.z/2)`，半尺寸 = `(size.x/2, size.z/2)`，`ClearInRectXZ(...)`（用外接圆会多清掉区域外的树）
  - 两处都在 `TerrainUtils.ModifyHeightMap` / `AdditionTerrain` **之前**清除；合并提交仍由 `LateUpdate` 限流完成
- 验证：两文件 `read_lints` 0 诊断（未在 Unity 编译/Play 验证；`ClearInRadius` 的 Y 轴默认不过滤，若要只清同一高度层的树可传 `yTolerance`）

## 【追加】LimitedLife 增加「延时回收」`EndDelay`
需求：增加延时回收字段，到点先触发 `OnEnd`，再等 N 秒才正式回收。只改 `Assets/Scripts/Effect/LimitedLife.cs`：

- 新序列化字段 `[InspectorName("延时回收(秒)")] private float EndDelay;`（默认 0 = 与旧行为完全一致）+ `SetEndDelay(float)` 运行时覆盖
- 新私有状态 `bool endInvoked`（本次存活是否已触发过 OnEnd）+ `private void InvokeEnd()`（`if(endInvoked) return; endInvoked=true; OnEnd?.Invoke();`）
- `IsAlive()`：`if (allowRelease) return false;`（**外部强制回收不走延时**，立即放行）→ `endTime = CreatTime + LiftTime`；未到 return true；到点先 `InvokeEnd()`，`return Time.time < endTime + EndDelay`
- `AllowPreRelease()` 阈值同步 `+EndDelay`，避免延时期间被同模板新请求"抢走"（提前释放会中断延时）
- `OnHide()` 改为调 `InvokeEnd()`（原来直接 `OnEnd?.Invoke()`）→ 未提前触发过才触发，杜绝重复
- `OnShow()`/`ResetLift()` 复位 `endInvoked`；`SetLift()` 若把寿命延长回存活期也复位（`ProjectileStandard` 的 trail 复用依赖这点）
- ⚠ 副作用知会：`SubtitleAirdrop.Update` 也用 `IsAlive()` 判存活，配了 EndDelay 的预制体空投字幕会多留 EndDelay 秒；`Health_AboState`/`WeaponController`/`DeployableMine`/`SpecUnitKei` 的强制回收仍即时
- 验证：`read_lints` 0 诊断；MCP `validate_script level=standard` 0 error 0 warning

## 环境变化
- MCP 实例 ID 变为 `Bluedivers@4a3e6a7b`（旧 `Bluedivers@3d9f2357` 已 not found；projectRoot 仍为 `D:/Pro/Bluedivers`，端口 6400）。`.codebuddy` 的多实例路由规则里的 ID 需按实时 `mcpforunity://instances` 更新

## 【咨询】石头/地形装饰物自动使用地图纹理：方案结论（未改任何代码）
需求：让 `ToonLit_Stone.shader` 的 `_BaseMap`/`_BlendingMap` 自动取 `MapData_SO.TerrainItem[0]/[1]`（随地图变），问"脚本注入保持现状"还是"新写 shader"。

结论（已答用户）：**脚本注入 + 保留现有 shader**；只有"石头要随脚下地形层自动变"时才需要动 shader，且也该在 Stone 自己的 HLSLPROGRAM 里开 `#define` 支路，而不是另写一套。

- shader 读不到 SO → 纹理必须由 CPU 侧喂；新写 shader 解决不了"自动取 MapData_SO"，只能解决"采样语义不同"（世界坐标/地形 alphamap）
- 证据：`ToonLit_Stone.shader`（guid `0032fd96d570be648bc8c86d33201d2d`）全仓**仅被 1 个材质**引用 `Assets/Rocks and Boulders 2/Rocks/Prefabs/新建材质.mat`（改动半径极小）；该材质本就 `_BaseMap`+`_BlendingMap` 双图（`_BlendingScale=0.857`/`_TerrainBlending=1`）
- 注入时机现成：`BattleManager.InitTerrain` 的 `GenerateNoiseTerrain.SetTextures`（BattleManager.cs:216-219）；5 层索引 0 草/1 沙/3 巢穴（被 `CampData_SO.NestTerrainItem` 覆盖）/4 岩（GenerateNoiseTerrain.cs:74-82、1029）
- 三条路：**A** per-renderer MPB（0 shader 改动，但要自己找 Renderer）；**B（推荐）** `Shader.SetGlobalTexture` 全局纹理 + Stone 里 `#define` 支路（全自动、不遍历场景、SRP Batcher 不受影响）；**C** 真·随脚下地形层变 → 才需要 shader 采样（把 GenerateNoiseTerrain 的 textureMap 烘小 RT + 全局 `_TerrainRect`，写法照抄 `SnowRendererFeature` 的 `_SnowMaskRect`）
- 坑：`TerrainLayer.tileSize`(世界米/tile) 与 `_BaseMap_ST`(uv 乘数) **量纲不同**，直接塞会满屏噪点，应按 `bounds.size.xz / tileSize` 换算；材质资产会被多地图互相覆盖 → MPB 或 `new Material`；全局纹理**不可写进 Properties**（否则材质值覆盖全局）；Stone 的 5 个 pass 共用 `GetFinalBaseColor`(ToonLit_Shared.hlsl:376) → 描边/深度/阴影裁切自动一致，别拆 pass；别往共享 `UnityPerMaterial` CBUFFER 加字段（影响所有 include 它的 shader）

## 【追加】B 方案"不越界"实现要点（`ToonLit_Shared.hlsl` 被 7 个 shader 共用）
共用该 hlsl 的 7 个 shader：`ToonLit` / `_Colour` / `_Face` / `_Hair` / `_MouthEye`（无 HLSLINCLUDE，只有 1 个 pass）/ `_Scene` / `_Stone`。

三条边界（做到即可零影响）：
- **编译单元边界**：宏不跨 .shader 文件泄漏 → `#define ToonLitStoneTerrainTex` 只写进 `ToonLit_Stone.shader:80` 的 `HLSLINCLUDE` 块（该块只属于此 shader，一次覆盖它的 5 个 pass，且早于各 pass 的 `#include`）。参考同类写法 `#define ToonShaderIsOutline`（因为只有部分 pass 需要，才写在 pass 内）
- **预处理器边界**：shared hlsl 只**新增**、不改既有语句 → ①`sampler2D _BlendingMap;`(86) 后新增 `#ifdef` 包裹的两个 `sampler2D` + 两个 `float4`；②`GetFinalBaseColor`(373) 里 `half4 backCol = tex2D(_BaseMap, input.uv);` 之后插入**early-return 支路**（新 albedo 算完直接 return，因此 `_UseMouthMap`/`_UseAlphaClipping`/`_BlendingScale` 那几行**一字不动**，未定义宏时字节码等价）
- **数据边界**：新数据只走**全局 uniform**（`Shader.SetGlobalTexture/Vector`），**不写进 Properties** → 不进 `UnityPerMaterial` CBUFFER → 不影响其它 shader 的 CBUFFER 布局；也**不要**给 `Varyings`/`Attributes` 加字段（采样所需的世界坐标 `positionWSAndFogFactor.xyz` 本来就有）

实现细节：
- 石头 uv 用 `input.positionWSAndFogFactor.xz`（世界 XZ），全局 `_TerrainTexTileA/B` = `(1/tileSize.x, 1/tileSize.y, -origin.xz/tileSize)`（地形 shader 的 uv = worldXZ/tileSize，故与地面**同密度**，顺便绕开 tileSize 量纲坑；MapRoot 不在原点时才需 zw 偏移）
- 混合比复用材质已有的 `_BlendingScale`（`lerp(texA, texB, _BlendingScale)`）
- 脚本侧 4 行放在 `GenerateNoiseTerrain.SetTextures`（`BattleManager.cs:216-219` 的调用链内）
- ⚠ 支路会绕过 Stone 的 `_UseMouthMap`/`_UseAlphaClipping`（该材质两者=0，当前无影响；以后石头若要溶解须在支路里补 `col.a`）
- 验证隔离：`git diff --stat` 只应有 2 个文件；编译后 `read_console` 0 error；随机开一个角色材质/场景材质外观不变；**反证法最直观**——临时把支路 return 改成 `half4(1,0,1,1)`，应只有石头变品红、其它模型不变
- 若想"一个字都不动 shared"（100% 符号隔离）：Stone 改用 `#pragma fragment ShadeFinalColorStone` + 新建 Stone 专属 hlsl（≈25 行，复用 shared 的 `Varyings`/`ToonSurfaceData`/`InitializeLightingData`/`ShadeAllLights`/`ApplyFog`），代价=与 shared 的 `ShadeFinalColor` 逻辑漂移（shared 后续改动不再自动跟随）

### 【已实施】用户确认只 SetGlobalTexture、不管 tiling（不用 SetGlobalVector）
改了 3 个文件，共 5 处（全部是"新增/ifdef 包裹"，无既有语句语义变化）：
- `Assets/Shader/ToonLit/Main/ToonLit_Stone.shader`：`HLSLINCLUDE`(80) 里加 `#define ToonLitStoneTerrainTex`（一次覆盖该 shader 5 个 pass）
- `Assets/Shader/ToonLit/Main/ToonLit_Shared.hlsl`：①`sampler2D _BlendingMap;` 后 `#ifdef` 包裹声明 `_TerrainBaseTex`/`_TerrainBlendTex`（**不进 Properties**）②`GetFinalBaseColor` 里 `backCol` 采样后 `#ifdef` 改成 `tex2D(_TerrainBaseTex, input.uv)`（uv 沿用顶点阶段已按 `_BaseMap_ST` 变换过的值 → **材质 Tiling/Offset 继续生效**，只换贴图来源）③`if(_BlendingScale)` 里 blendMap 那行改成 `#ifdef` / `#else` 双分支（`_TerrainBlendTex` / 原 `_BlendingMap`）
- `Assets/Scripts/08Map/GenerateNoiseTerrain.cs` `SetTextures` 开头 2 行：`Shader.SetGlobalTexture("_TerrainBaseTex", Texture[0])` / `("_TerrainBlendTex", Texture[1])` → 即 `MapData_SO.TerrainItem[0]/[1]`（索引语义：0 草/1 沙/2 ?/3 巢穴被覆盖/4 岩）
- 映射：**第 1 张→`_BaseMap`，第 2 张→`_BlendingMap`**（混合比仍由材质 `_BlendingScale`/`_TerrainBlending` 控制）
- 验证（MCP，实例 `Bluedivers@4a3e6a7b`）：`refresh_unity(mode=force)` 后 `ShaderUtil.GetShaderMessages` 逐个查 7 个 ToonLit shader → `ToonLit.shader` 与 `_Stone` 均为**同样 2 条 D3D Warning**（`ToonLit_Shared.hlsl:421/419/485` "implicit truncation of vector type"，改动前就有）→ **0 error、无新增诊断**；`read_console` 仅 3 条既有的 "inconsistent line endings" 警告（实测新增行都是 CRLF，LF-only 行数：Shared.hlsl 6 / Stone 1 / **未改动的 ToonLit.shader 也是 1** → 属该包既有问题）；`git diff --numstat` 无整文件重写；`read_lints` 0 诊断
- 注意：采样改读全局纹理后，材质上的 `_BaseMap`/`_BlendingMap` 贴图槽位对该材质**不再生效**（仅 Tiling/Offset 仍生效）；全局纹理未设置时是默认灰/白
- 追问"是不是只有 ForwardLit/Outline 需要这个宏"的结论：Stone 的 5 个 pass **都会调** `GetFinalBaseColor` —— ForwardLit(163)/Outline(210) 走 `ShadeFinalColor`（要 albedo），ShadowCaster(239)/DepthNormalsOnly(270)/DepthOnly(298) 走 `BaseColorAlphaClipTest`(Shared:711) **只取 `.a`**；而 `DoClipTestToTargetAlphaValue` 仅在 `_UseMouthMap`/`_UseAlphaClipping` 开时才 clip，石头材质两者=0 → 后 3 个 pass 里那段支路**对画面零影响**（等于白采样 1~2 次）。保留全 5 pass 的唯一理由是"以后石头启用溶解时，投影/写深度的裁切必须与 ForwardLit 同图"，否则会出现"本体溶解了还在投影/挡视线"。若要省这点采样：删掉 HLSLINCLUDE 里那行，改为在 ForwardLit/Outline 两个 pass 的 `#include` 前各写一行 `#define`（照 `ToonShaderIsOutline` 的写法）。另一个可顺手优化：支路里 `tex2D(_BaseMap,…)` 先采后被覆盖，建议改成 `#ifdef/#else` 给 `backCol` 分别赋值，消除可能存在的无用采样

### 【已执行 2+3】宏精简为两个 pass + 消除死采样
- `ToonLit_Stone.shader`：`HLSLINCLUDE` 里的 `#define` 移除（原地留注释说明"溶解时必须给 3 个深度/阴影 pass 补回"），`#define ToonLitStoneTerrainTex` 改写进 **ForwardLit**(168) 与 **Outline** 两个 pass 的 `#include` 前（同 `ToonShaderIsOutline` 的按 pass 精确控制写法）
- `ToonLit_Shared.hlsl` `GetFinalBaseColor`：`backCol` 改为 `#ifdef/_#else` 分别赋值，消除"先采 `_BaseMap` 再被覆盖"的潜在无用采样；未定义宏的 shader 走 `#else`，与原语句完全一致
- 验证（MCP）：`ShaderUtil.GetShaderMessages` → 其它 6 个 shader 仍 2 条既有 Warning(Shared 422/486)，**Stone 变 3 条**(420/422/486)：420=ifdef 分支（被 ForwardLit/Outline 编到）、422=else 分支（被深度/阴影 3 个 pass 编到），全是同一种 "implicit truncation of vector type"（`half3 blendMap = tex2D(...)` 的既有问题）→ **0 error、无新增诊断类型**
- **`[HideInInspector]` 结论：不加**。原因：①`[HideInInspector]` 会连 Tiling/Offset 一起藏掉，而 `_BaseMap_ST`（顶点 `TRANSFORM_TEX` 决定采样 uv）与 `_BlendingMap_ST`（混合 uv）**仍在生效** ②隐藏只影响绘制不删属性，`Material.mainTexture`（`[MainTexture]`）仍有效，材质资产里的贴图引用也还在 ③family 其它 6 个 shader 都显示这两个槽位，Stone 单独隐藏会成特例。要提示"由地图注入"改**显示名**或写注释即可；在意包体可把材质里这两个槽位的**贴图清空**（Tiling 值保留）
- ~~可选未做：给 `half3 blendMap = tex2D(...)` 补 `.rgb`~~ → **已做**：`ToonLit_Shared.hlsl` 两处补显式截断（`blendMap` 两个分支 `tex2D(...).rgb`、`GetFinalColourColor` 里 `_ColourColor.rgb`）→ `ShaderUtil.GetShaderMessages` 复查 **7 个 shader 全部 msgs=0**（truncation warning 清零，无 error）

### 【审计】ToonLit_Stone.shader 可删参数清单（已答复，未删）
- **A 纯死参（.hlsl 里 0 引用，删了零视觉变化）**：`_IndirectLightMultiplier`、`_DirectLightMultiplier`、`_MainLightIgnoreCelShade`、`_AdditionalLightIgnoreCelShade`（NiloCat 原版遗留；全仓只在各 shader 的 Properties、`.mat`、以及 `Shader/Editor/ToonLitShaderGUI.cs` 里出现）。⚠ **别在 `ToonLit.shader`(main) 里删** —— 它挂 `CustomEditor "ToonLitMainShaderGUI"`，而 `ToonLitShaderGUI.cs:325-329` 是 `FindProperty(...)` 后**不判空**直接 `ShaderProperty`；Stone/Scene/Face/Colour 的 CustomEditor 都被注释掉（走默认 MaterialEditor）→ 安全
- **B 当前值即中性值、可删但失去调参**：`_ReceiveShadowMappingPosOffset`(0)、`_FixOutlineColor`(0)、`_UseAverNormal`(0)（属性不在 Properties 里时 CBUFFER 取 0，与现值一致）
- **C 看着能删其实不能（删了变黑/失效）**：`_BaseColor`/`_BaseScale`(→0 全黑)、`_FogMaxValue`(→0 不吃雾)、`_IndirectLightMinColor`(→0 暗部可能全黑)、`_ShadowMapColor`(→0 暗面纯黑)、`_CelShadeMidPoint`/`_CelShadeSoftness`、`_OutlineWidth`/`_OutlineColor`/`_OutlineZOffset`
- **D 结构型不能删**：`_BaseMap`/`_BlendingMap`（贴图槽位对 Stone 半死，但 `_BaseMap_ST`/`_BlendingMap_ST` 还活着：顶点 uv 变换 + 混合 uv）、`_RenderRef`(Stencil Ref)、`_MAIN_LIGHT_SHADOWS`(`#ifdef` 在用)
- **E 顺带可清的非参数**：注释掉的 `_OutlineZOffsetMask*` 3 行与 `_IsFace`、`HLSLINCLUDE` 里 4 行注释 pragma、5 个 pass 重复的说明注释；另 `GetFinalSpecular` 无条件 `tex2D(_SpecularMap,…)`，而 `_SpecularMap` 在 Stone 的 Properties 里没有（未绑定采样），结果只被 `_UseSpecular`(=0) 分支使用 → 每像素白采样，要省需在 shared 里加条件

### 【已执行 A+B+E】ToonLit_Stone 参数瘦身 + 注释清理
`ToonLit_Stone.shader` 的 `Properties` **25 → 17 条**：
- 删 A 组 4 个死参 `_IndirectLightMultiplier` / `_DirectLightMultiplier` / `_MainLightIgnoreCelShade` / `_AdditionalLightIgnoreCelShade`
- 删 B 组 3 个（当前值=未声明时的默认 0，**视觉等价**）`_ReceiveShadowMappingPosOffset` / `_FixOutlineColor` / `_UseAverNormal`（⚠ 后两个原本在 Inspector 可见，删后无法再开"固定描边色/平均法线描边"）
- 保留仍被读取的：`_BaseColor`/`_BaseScale`/`_ShadowMapColor`/`_FogMaxValue`/`_IndirectLightMinColor`/`_CelShadeMidPoint`/`_CelShadeSoftness`/`_ReceiveShadowMappingAmount`/`_OutlineWidth`/`_OutlineColor`/`_OutlineZOffset`/`_BaseMap`/`_BlendingMap`（后两者 `_ST` 仍在用）/`_RenderRef`/`_MAIN_LIGHT_SHADOWS`/`_BlendingScale`/`_TerrainBlending`
- E：清掉注释掉的 `_IsFace`/`[Header(High Level Setting)]`、3 行 `_OutlineZOffsetMask*`、`HLSLINCLUDE` 里 4 行注释 pragma（块内改成一句说明）、5 个 pass 里重复的 `//在此内编写的所有着色器逻辑…`、ForwardLit 里已过时的"不需要任何特殊的#define"、ShadowCaster/DepthNormalsOnly 里已过时的 `_UseAlphaClipping 已在 HLSLINCLUDE 定义`；`HLSLINCLUDE` 前面那句"提取重复代码"的模板注释也换成地形宏的真实说明
- 额外一处真实浪费：`ToonLit_Shared.hlsl` 的 `GetFinalSpecular` 改为 `if (_UseSpecular)` 内才采样 → 未声明 `_SpecularMap` 的 shader（Stone 等）不再每像素白采一张未绑定贴图，行为不变
- 验证（MCP）：`ShaderUtil.GetShaderMessages` **7 个 shader 全 0 诊断**；`Shader.GetPropertyCount`：Stone=17（原 25）、main=53、Scene=44；`新建材质.mat` 关键值仍在（`_BaseColor`=白、`_ShadowMapColor`=(0.8,0.8,0.8,1)、`_BlendingScale`=0.51(用户在手改)、`_TerrainBlending`=1、`_ReceiveShadowMappingAmount`=0.65、`_FogMaxValue`=1、`_OutlineWidth`=10、`_CelShadeMidPoint`=-0.5），已删的 4 项 `HasProperty`=False、`_SpecularMap`/`_Colour`/`_IsFace` 本来就没有
- 观察：该材质运行时 `GetTexture("_BaseMap")`/`GetTexture("_BlendingMap")` 与 `mainTexture` 均为 null，但磁盘 YAML 里仍有贴图 guid（`d0a42dcb…`/`98f2daaa…`）且 `GetTextureScale`=(3,3) → 疑似贴图资源缺失或贴图槽对石头已无意义，与本次 shader 改动无关

